# RC completion matrix

Updated: 2026-08-02
Overall status: `HUMAN_PLAYTEST_PENDING`
Automated productization baseline: `FROZEN`
Product version: `0.3.1`
Previous immutable baseline: `mcac-productization-baseline-0.3.0`
Current baseline tag: `mcac-productization-baseline-0.3.1`
External follow-up:

- `HUMAN_PLAYTEST_PENDING`

Machine-readable current product facts: [product/PRODUCT_TRUTH.json](product/PRODUCT_TRUTH.json).
Historical candidate SHAs and workflow run IDs below apply only to their named revisions.

## Evidence scope

### Historical live evidence

On 2026-08-01, a disposable PCL + Forge 1.20.1 + Runtime + Hermes + official
DeepSeek API vertical slice verified handshake/recovery, world-state reading, Follow lifecycle,
Navigate position change, safe idle, and reconnect. It is retained as historical evidence and is
not reused as current 2026-08-02 proof. This bounded result does not establish Fabric, other-provider,
broad Tool/terrain, or sustained subjective human-play coverage.

### Current closeout evidence

The newly built Forge artifact was formally installed into
the real PCL Forge 1.20.1 instance and its SHA-256 matched the build. Forge startup established the
Runtime handshake and one live session without the prior `ResourcePackInfo`/missing-`pack.mcmeta`
warning; real UI commands exercised status, Follow, Navigate, Pause, Resume and Stop, including a
successful nearby Navigate and an honestly surfaced unreachable-path pause. Official DeepSeek was
not invoked in this closeout because its credentials were absent from the process environment, and
OpenRouter was not used.

## Current three-layer validation model

| Layer | Current contract | State |
|---|---|---|
| 1. PR fast | Every PR update runs one shared `check` plus parallel history-secret and dependency-diff review; superseded updates are cancelled | `DEFINED_PENDING_REMOTE` |
| 2. Ready-PR Windows integration | Relevant non-Draft PRs run Windows link/installer boundaries, browser product E2E, package verification, launchers, HTML startup, and clean-extraction Golden Path only when opened, reopened, or deliberately moved to Ready | `DEFINED_PENDING_REMOTE` |
| 3. Audit and release candidate | Weekly/manual audit runs Heavy Loader/Runtime, Secret/CodeQL, and Gradle 9; manual `candidate` mode is exact-`main` only, waits for every audit job, then builds and uploads the single verified RC | `DEFINED_PENDING_REMOTE` |

There is no automatic duplicate validation on ordinary `main` pushes and no per-commit candidate
artifact. Iteration stays in Draft; Layer 2 is a deliberate integration boundary, and a substantial
post-Ready change must return to Draft before another Ready transition. Failures are diagnosed from
their evidence and receive focused revalidation instead of automatic full-chain restarts.

The prior four-workflow model remains historical evidence: candidate
`a1ca550d917db9c67704706e6f4244789a17a4fe` passed PR fast `30440261845`,
supply-chain/CodeQL `30440261942`, Windows `30440261982`, and manually dispatched Minecraft-heavy
`30440268469`. Those runs do not claim verification of the new three-layer trigger contract.

Final-gate stability note: menu capabilities now honor their documented monotonic 60-second
wall-clock lifetime even when GameTest ticks are accelerated, and Task Graph `await()` exposes a
terminal result only after the idempotent terminal lifecycle event is durable. Boundary assertions,
five repeated targeted lifecycle-ordering runs, and repeated Runtime/Fabric E2E cover these fixes.
The deterministic external-client E2E also waits for its declared connected-body Tool set through
authenticated MCP `tools/list` before Graph submission, removing a registration/capability-publication
race without weakening `TOOL_UNAVAILABLE` validation.
The v5.2 final gate additionally replaced fixed replay-provider startup sleeps with bounded
process-and-loopback-listener readiness checks for both planning and Hermes fixtures; two consecutive
full Runtime/Fabric executions covered the cold-start failure point without changing Runtime fallback
or Provider validation.
The menu GameTest fixture also keeps its real chest one block inside the owning parallel test cell,
preventing cross-cell visibility interference while preserving all token, vanilla click, quick-move,
inventory-delta, close, and invalidation assertions.
The lifecycle GameTest now verifies the exact vanilla diamond death drop and then removes that
fixture-owned entity before later batches run, preventing real pickup behavior from contaminating
the independent delivery inventory-delta assertion.
The Windows RC artifact upload includes the release `SHA256SUMS.txt` alongside the ZIP, sidecar,
Manifest, and SBOM, so the downloaded artifact exposes every v5.2 integrity record without requiring
the reviewer to extract the release ZIP first.
The productization-freeze safety rescan replaced path-keyed process-lifetime installer/profile lock
maps with fixed lock stripes, bounded both asynchronous player-planning queues with explicit
backpressure, capped and expires HTML-window tracking, and made the Fabric vanilla-drop fixture
contain randomized pickup motion without increasing its timeout or weakening its assertions.
Focused Runtime and Terminal suites, including explicit queue-saturation tests, pass locally; this
does not change any Live Brain or human-play evidence label.
The Skill revocation isolation test retains its three-second persisted wait and exact cross-workspace
cancellation assertions while allowing a bounded eight-second observation window under the full
parallel suite, avoiding a one-second scheduler-margin race without changing Runtime timeouts.
Generated Skill trials now use migration 28 leases bound to the exact Profile, Companion,
controller and Brain session. They are single-use, short-lived, limited to explicit LOW-risk Tools
and tighter Task Graph budgets, execute through the persistent Graph Runtime, retain bounded
terminal evidence, can be revoked immediately by authenticated local management, and become
non-resumable `REVOKED` records after a Runtime restart. This local automation is not Live-Hermes
or human-play evidence.
Fabric and Forge now persist the last accepted control epoch plus active behavior identity and
revision in Loader saved data. Disconnect and Loader startup leave unfinished work safely `PAUSED`;
same-or-lower epochs are rejected, while a higher epoch reacquires the same paused behavior for
explicit resume. Runtime also advances its durable epoch floor from each authenticated Mod status
snapshot, so a Runtime database behind the world save cannot replay old authority. Dual-Loader
GameTests cover NBT round trips, disconnect/reacquire/resume, and replay rejection; the complete
Runtime/Fabric E2E additionally covers Runtime restart and abrupt-crash recovery. These are not
claims of Live/Human evidence. Separate real dedicated-server two-process gates now stop and restart
both supported Loaders against the same world directory, recover the same Companion UUID/body and
inventory, and quarantine the interrupted navigation as `PAUSED`.
The first Compatibility Host vertical slice adds a bounded `.mcac-compat` archive loader, immutable
content-addressed Store, signed active index, journal recovery, environment fingerprinting,
deterministic layered resolver, scoped authorization, lifecycle management API and the existing
Terminal's compatibility page. Archive traversal, hash/index tampering, scope escape, dependency
failure, corrupt-pack isolation and every injected index-switch crash window fail closed in local
tests. Built-in Fabric/Forge Full Bridge and NeoForge `LOCAL_ONLY` descriptors describe existing
Loader bodies without rewriting them. Fixture evidence proves this framework only; it is not a
claim that arbitrary third-party Mods are compatible.

External follow-up: `LIVE_BRAIN_EXTERNAL_VERIFICATION_PENDING`, `HUMAN_PLAYTEST_PENDING`.
Forge 1.20.1 Full Runtime Bridge is a required gate under the current dual-loader execution
contract. NeoForge remains `LOCAL_ONLY`.

The Web Terminal now has `zh-CN` and `en-US` resources across navigation, pages, buttons,
descriptions, empty/loading/error states, confirmation/progress surfaces, tables and ARIA labels.
It selects the system language initially, persists an explicit choice, switches without reload or
state loss, fails fast on missing keys in development/tests, and maps backend failures to a
selected-language explanation while retaining the technical code. Terminology is governed by
`docs/product/TERMINOLOGY.md`. A bilingual Chromium path now passes against the real packaged Java
Terminal and Runtime with an isolated PCL2 fixture and no `fetch` mocks. It covers navigation,
locale/theme/instance state, install/update/repair/rollback/uninstall, Runtime lifecycle and token
rotation, session/log/support operations, plus isolated v1/v2 compatibility-pack install and
update, honest `STAGING` retention without a manual evidence button, quarantine and removal.
Compatibility Host unit automation separately covers evidence/index/activation/rollback/disable.
Fresh screenshots were checked
for overflow, stale Loader metadata and leaked `undefined` values. The production TypeScript build,
Web lint, 12 Web test files / 18 tests, Compatibility Host tests and Terminal tests pass locally.
This is local browser automation, not Live Hermes or human-play evidence.
Documentation now has a single reader-facing status page, sole evidence matrix, bilingual user
guides, developer entry and historical archive boundary. The strengthened documentation gate and
the repository productization audit pass alongside secret, dependency-pinning and npm production
audit checks; exact final-candidate release and remote results are still required.
Final-candidate preflight also removed two fixture ambiguities without weakening product behavior:
the Fabric restart seed now uses continuous FOLLOW navigation so accelerated GameTest ticks cannot
finish a finite route before shutdown, and launch-test preparation declares disabled/persistence
mode properties as inputs so Gradle cannot reuse a fixture prepared for another mode. Focused
Fabric/Forge restart, Runtime-disabled launch and multi-Profile gates pass.
The same preflight exposed a protocol mismatch when a Loader published an honestly blocked
behavior: the sender included its required machine-readable failure detail, while the protocol
model admitted details only for terminal `FAILED`. `BehaviorEvent` now requires bounded
failureCode/message details for both `BLOCKED` and `FAILED` and continues to reject them for
non-failure states. Protocol tests and the full unknown-Mod Runtime/Fabric chain pass without an
unexpected severe log.
Final-candidate Runtime/Fabric validation also exposed a Loader lifecycle revision regression:
an internally observed safety pause published revision `n+1` without advancing the persisted
Loader control revision, so a later explicit pause could return the older `n`. Both 1.21.1 and
1.20.1 common registries now advance and persist every published lifecycle revision and treat
each accepted explicit pause as a new monotonic lifecycle event. The owner-goal-modification
Replay fixture now uses continuous Follow control instead of an intentionally out-of-range
destination, eliminating a fixture-created `TARGET_OUT_OF_RANGE` race without weakening the
pause, supersede, cancel and same-plan activation assertions. The complete Runtime/Fabric and
Runtime/Forge E2E paths pass after these corrections.

Status values are limited to `NOT_STARTED`, `PARTIAL`, `IMPLEMENTED`, `LOCALLY_VERIFIED`,
`REMOTELY_VERIFIED`, and `BLOCKED_BY_EXCLUDED_LIVE_TEST`. Replay evidence is not Live evidence.

2026-07-29 corrective audit: Brain configuration is now independent from legacy Provider settings;
production no longer constructs the internal Agent/Provider planner; durable Minecraft/Task Graph
starts return non-completion receipts and are inspected asynchronously; action execution deadlines
are distinct from request acknowledgement; Brain health requires a protocol Tool-call probe; log
streaming uses bounded tail/incremental reads; waiting answers validate the active Brain session
before persistence; and OpenAI-compatible requests serialize per session rather than per adapter.
The Terminal can no longer submit user-authored `passed=true`/`EXACT_VERIFIED` Compatibility
evidence; Fabric and Forge now share the same bounded reconnect-stable conversation-event dedupe
window; and a `VERIFIED` Brain completion claim must satisfy typed observation-field conditions
rather than merely cite any successful observation.
The same corrective audit found and closed four local integration gaps without changing the Live
evidence label: the reconnect-stable delivery window is compiled directly into both production
Loader JARs and checked at artifact time; a disabled external Brain no longer crashes the Runtime
CLI after its listeners start; asynchronous Tool receipts can be verified after completion through
companion-scoped `task.inspect(taskId)`; and Runtime E2E readiness now requires both a live process
and an authenticated health response rather than token-file creation alone. Real Forge 4/4 and
Fabric 31/31 GameTests, authenticated Runtime E2E on both Loaders, the Replay Hermes ASK_USER chain,
and the real-backend Chromium product flow pass locally. Installed personal PCL2/HMCL roots were
discovered read-only, but no MCAC Mod was present in those instances and no account-bound launcher
or human world was started.

| Module | Status | Implementation evidence | Test evidence | Remaining gap |
|---|---|---|---|---|
| Compatibility Host and Pack Lifecycle | LOCALLY_VERIFIED | A declaration-only `.mcac-compat/1` contract is loaded without extraction under fixed 8 MiB archive, 256-entry, 1 MiB per-entry and 16 MiB actual total-inflated-byte limits; exactly one YAML manifest name is accepted; JSON/YAML reject duplicate keys, trailing/multiple documents, excessive depth/name/number/string/token sizes and YAML aliases; strict path/hash/field validation remains in force; immutable objects, signed active index, journals, snapshots and state directories support install, evidence, index, activate, disable, update, patch, rollback, quarantine, remove, export and recovery under an exact controller/Profile/instance/Store/operation/expiry/risk grant; fingerprints bind Minecraft, Loader, Java, bounded Mod hashes and configuration/script/datapack/manifest hashes; deterministic precedence composes base, Loader, Mod, patch, overlay and instance packs, and suppresses high-risk stale/provisional/structural capabilities; dynamic in-process native execution remains explicitly closed; Terminal management exposes install/index/activation/lifecycle operations but cannot submit user-authored `passed=true` or `EXACT_VERIFIED` evidence, so a Pack remains `STAGING` until trusted automation supplies a real result | lifecycle automation covers an isolated unknown-Mod fixture from install through system-authored evidence/index/activation, update, rollback and disable; overlay precedence and stale-fingerprint suppression are verified; injected failures after object write, candidate index and active-index switch all recover the complete prior index; hostile-input tests cover highly compressible aggregate overflow, near-limit entries, dual manifests, duplicate JSON/YAML keys, deep nesting, huge scalars, aliases, multiple documents and truncated ZIPs; existing corrupt archive, traversal, duplicate identity/content, dependency, evidence, signed-index, scope, native boundary and cross-instance tests remain green; compatibility module and Terminal tests pass | Fixture and local browser automation are not arbitrary third-party-Mod, Live Brain or human-play evidence; a trusted external test runner/API for real packs, native extension dynamic execution and broader field pack corpus remain future work |
| Forge 1.20.1 Runtime Bridge | LOCALLY_VERIFIED | Forge starts an optional reconnecting authenticated `mc-companion/1` WebSocket adapter, uses a small Gson-backed bridge JSON compatibility layer and excludes Jackson class references from the production Mod artifact, publishes bounded ServerPlayer status/inventory/vitals, enforces persisted monotonic epoch leases, routes follow/travel/return and pause/resume/cancel through the version-common deterministic registry, retains behavior identity/revision across disconnect, and requires explicit higher-epoch resume from safe `PAUSED`; live Registry/recipe/block/item/entity/menu observations are implemented; bounded vanilla ServerPlayer executors cover movement, interaction, inventory, collection, mining, crafting, smelting, combat and safety; `/mcac` player text and exact block-use/break owner activity use the authenticated Runtime session; Fabric and Forge compile the same bounded reconnect-stable conversation-event delivery window directly into their production artifacts, so a resend whose ACK was lost is acknowledged without being displayed twice; the shared navigation core supplies bounded deterministic routing and re-planning; Terminal treats exactly Fabric 1.21.1 and Forge 1.20.1 as Full Bridge targets | pure-core tests prove first delivery, duplicate rejection and bounded eviction for the shared window; Loader artifact checks require both the Runtime adapter and delivery-window class in the production JAR; four real Java 17 Forge 47.4.10 GameTests cover the advertised primitive/composite surface, lease/reconnect lifecycle, live observations, persistence/death recovery, authenticated Runtime control, direct token-bound vanilla menu clicks/quick-move/close, the navigation matrix, and isolated unknown-namespace Registry/tag/recipe discovery; a production-JAR inspection excludes the fixture; a separate normal dedicated-server two-process gate at `e6b81e4` preserves one world across clean stop/restart and verifies the same Companion UUID, live body and diamond inventory plus safe `PAUSED` quarantine of the interrupted route; authenticated Runtime/Forge E2E verifies registration and FOLLOW/pause/resume/cancel through the real bridge; PCL2/HMCL tests detect isolated Forge 1.20.1 with Java 17; a clean production-service lifecycle fixture verifies exact Forge artifact selection, pairing config, repair, update, rollback, both uninstall policies, and preservation of worlds/unrelated Mods; Doctor enumerates Forge Bridge, Runtime, MCP, Registry, navigation and external-Brain status | The 2026-08-01 PCL/Hermes/official-DeepSeek vertical slice is live-provider verified; subjective human play, other providers/Loaders, process-level lost-ACK injection, broader dual-loader failure injection and production-scale field evidence remain follow-up work |
| Fabric + Forge Global Navigation | PARTIAL | A shared bounded deterministic A* core accepts only externally selected destinations and delegates version-specific collision, support, hazard, water, climbable, door and live-entity observations to thin 1.20.1/1.21.1 adapters; both Loader directors re-plan on target movement, route invalidation and lack of progress, retain timeout/pause/cancel control, keep the Owner reachable for follow semantics, reject non-finite coordinates before state mutation, and report `TARGET_CHUNK_UNLOADED`, `TARGET_OUT_OF_RANGE`, `PATH_UNREACHABLE`, or `STUCK` instead of fabricating arrival; the navigator owns no block-break/place executor, so structures, containers, redstone, valuable blocks and unknown Mod blocks remain obstacles unless the external Brain separately invokes permission- and budget-bound primitive Tools | pure-core tests cover deterministic obstacle routing, lower observed traversal-cost selection, unloaded targets and unreachable targets; Fabric 31/31 and Forge 4/4 real-server GameTests cover long travel, vanilla door opening, stairs/jumping, vertical swimming, ladder and vine climbing, stationary entity avoidance without harm/displacement, a block inserted during execution, mid-route pause/resume, magma-route avoidance without health loss or fixture mutation, response to a moved follow target, explicit owner stop to stable `IDLE`, non-finite target rejection without mode change, an actually unloaded target that remains unloaded and pauses with `TARGET_CHUNK_UNLOADED`, unchanged fixtures, observed arrival, unreachable failure and movement-stop regression; the same Loader bodies then complete a navigation-to-collection-to-mining-to-container sequence with exact entity/world/source-container/body-inventory deltas; real two-process dedicated-server gates on both Loaders recover UUID/body/inventory and quarantine interrupted navigation as `PAUSED`; authenticated Fabric and Forge Runtime E2E emit bounded owner block-activity events, Runtime target-binding tests reject unrelated activity, and Brain-coordinator tests prove matching activity pauses the old Tool before a short instruction; the full Runtime/Fabric E2E also proves same-plan in-flight goal modification; these are layered automated tests, not a human click claim | `HUMAN_PLAYTEST_PENDING` for passive physical takeover feel and real mixed-terrain route quality |
| Primitive Tool Coverage | PARTIAL | Existing gateway exposes bounded world observation/scan, movement, composite resource/inventory/item/combat actions and deterministic task controls; read-only primitives add bounded `world.query`, `inventory.inspect`, `safety.inspect`, `task.inspect`, dynamic `capability.list/describe`, capability-gated live `registry.search/describe` plus `recipe.query`, and connected-body `block.inspect`, `item.inspect`, `entity.inspect`, and `menu.inspect`; bounded mutation/body-control entry points add `movement.step/look/stop`, `block.break/interact/place`, `entity.collect/interact/attack`, bidirectional `inventory.transfer`, short-lived exact-session `menu.click/quick_move/close`, `item.use`, `inventory.drop`, `safety.retreat`, persistent nonblocking `task.wait`, and boundary-safe `task.checkpoint` without adding scenario Handlers, reusing durable lease/task state and vanilla PlayerAction/menu/use/drop/attack/retreat executors; menu capabilities use a monotonic 60-second wall-clock lifetime and still invalidate on menu replacement or close; placement, attack and explicit retreat receive exact external targets, with no internal build/combat strategy | Runtime primitive tests cover unknown namespaced inventory/container data, observation envelopes, hazard flags, formal-to-connected capability visibility, query capability gating, argument/session/permission binding, cancellation, persistent wait/checkpoint controls and full MCP-to-authenticated-Mod Registry/Observation result routes; Task Graph validation accepts the real `INTERACT`/`BUILD` permissions; 23/23 real Fabric GameTests cover the shared body executors, live unknown-namespace Registry/recipe queries, visible live block/item/entity/menu inspection, verified vanilla body rotation, exact vanilla block placement and item consumption, chest/entity/menu interaction, externally selected vanilla living-entity damage and threat retreat, automatic reflex retreat, opaque-menu-capability rejection, exact wall-clock expiry boundaries, item-use projectile creation, and exact world-drop/inventory deltas; three real Forge GameTests now independently cover exact placement/use/entity interaction/attack and two-block connected-vein effects in addition to Registry, recipe, menu, inventory, movement, combat and safety paths; repeated production Runtime/Fabric E2E submits generic `inventory.transfer` withdraw/deposit and `block.interact` → `menu.inspect` → token-bound `menu.close` graphs, then the later shortage chain proves the same chest still contains and delivers exactly six items | Direct per-alias cross-loader Brain E2E, cancellation/budget breadth and remaining composite equivalence |
| Task Graph Schema | LOCALLY_VERIFIED | `mcac-task-graph/1` JSON/YAML codec; all required declarative node types; stable node IDs; typed inputs; permissions; bounded limits including separate Evidence entry/byte ceilings; JSON object and real `document+format` JSON/YAML Tool ingress | YAML/JSON parsing, Tool-ingress YAML execution, duplicate-key, oversized-document and invalid Evidence-byte-limit tests | Remote verification and schema migration/versioning |
| Task Graph Validator | LOCALLY_VERIFIED | strict unknown-field rejection, safe expression parser without eval, unique IDs, contextual Tool availability, explicit current-Runtime node executability, graph-to-current `ToolDefinition.permission` and `inputSchema` binding, unknown-permission rejection, bounded literal array-reference validation, nested-parallel rejection and hard ceilings | Runtime integration verifies unsupported-node, missing/mismatched/unknown Tool permission, required/type/range/enum/additional-property schema mismatches, safe deferred references, malformed/dynamic/over-limit array reference and nested-parallel rejection plus prior fuzz-shaped boundary cases | expression/schema validator fuzz/property coverage and remote verification |
| Task Graph Runtime | PARTIAL | `task_graph.execute` creates a persistent asynchronous execution; `inspect/pause/resume/cancel` are session-owned controls and external cancel results preserve the request `callId`; the authenticated local Terminal additionally lists bounded same-companion safe summaries and derives pause/resume/cancel ownership from the durable record without exposing graph inputs/Tool results or adding graph creation/planning; ready executions use a fair bounded Runtime queue with explicit 64-per-Runtime/Profile, 16-per-Brain-session, and 24-per-Companion admission limits; overload returns `QUEUE_FULL` or `ADMISSION_REJECTED` before creating an unallocated durable execution, and elapsed timed waits retain `WAITING` while retrying bounded admission; timed wait/retry deadlines persist, release the two fixed Graph workers, resume through a separate bounded scheduler, and are re-registered after restart; WAITING time records remain query/pause/cancel capable and resume only when state and deadline still match; graceful close drains a worker that already persisted WAITING before stopping timers; retry cursors advance through stable scoped call IDs, and validation plus execution reject automatic retry around a currently non-idempotent Tool; timeout requests cancellation of every active child Tool, waits for bounded durable confirmation, and persists unconfirmed cancellation as `RECONCILIATION_REQUIRED`; recovery `resume` rechecks graph hash, persisted state shape, current Tool availability/permission/schema/idempotency and owner binding before leaving reconciliation; the current scoped node key is durable, and a confirmed Tool result must match the exact execution, nested loop scope, attempt and Tool before a non-idempotent boundary can resume; before replay is considered, the Gateway may import a proven existing terminal result without dispatch, and the Minecraft implementation binds it to the original request hash, Brain session, companion, call ID, durable task/behavior observation and released control epoch; deterministic nodes now include `if`, `switch`, `repeat`, `while`, durable `ask_user`, permission-bound `read_memory`, and quarantined `suggest_memory`; safe expressions read inputs, persisted state and prior Tool observations without eval; array `.length` and literal `[0..255]` selection compose observations into later Tool arguments; resolved arguments are revalidated against the current Tool schema immediately before dispatch; all graphs share one Runtime-owned four-worker parallel pool backed by a fair bounded 64-entry queue with caller-side backpressure, each graph/node is capped by `maxParallelNodes`/`maxConcurrency`, nested parallel is rejected, and close confirms worker/timer termination; stable scoped Tool IDs, runtime permission recheck, dedupe, serialized concurrent snapshots, unified Evidence entry/byte rotation, durable node outputs and terminal return values are enforced; oversized Evidence entries retain explicit size/hash metadata without raw content; Runtime performs no planning and Task control Tools cannot be called recursively by graphs | asynchronous start/progress/inspect, YAML, safe expression, input/output/state and array reference, static and resolved Tool-schema rejection without dispatch, unified progress/Tool/checkpoint Evidence byte+entry budget, unknown-namespace scan-candidate-to-navigation composition, conditional/switch branching, bounded loop exhaustion, two-Graph shared global concurrency cap, nonblocking dual-wait worker-release, persisted deadline restart, persisted retry-attempt restart without replay, WAITING pause/cancel, graceful-close WAITING preservation, non-idempotent retry rejection, true concurrent branch entry, all-active-Tool cancellation, confirmed wait cancellation, unconfirmed Tool cancellation quarantine, external cancel call identity, typed memory filtering/permission denial and suggestion isolation, durable game/HTTP answer routing, terminal-result query, pause/resume, idempotent restart recovery, exact nested-loop call IDs, exact-scope confirmed non-idempotent result reuse without replay, prior-iteration rejection, request-hash/session/companion/call-bound durable Minecraft task result import, injected repository failure at the post-effect/pre-result boundary followed by Runtime replacement and exact durable result import without replay, abrupt live Runtime/Fabric process crash at the same boundary followed by startup quarantine, reconnect and no-replay durable result import, corrupt-state rejection, duplicate call ID and cross-session isolation tests; local-management repository/Runtime tests prove summary redaction, companion isolation and pause/resume; Runtime HTTP integration covers list/control and the Companion-page Vitest covers visible progress/control dispatch; the crash E2E independently verifies one command binding and one behavior run; bounded-admission saturation holds both Graph workers, admits 64 executions distributed across four sessions/companions, rejects the seventeenth same-session and sixty-fifth global request without durable orphan state, preserves FIFO order, and completes every accepted execution; a separate cap test proves one Companion cannot consume another Companion's capacity; the complete Runtime test suite passes | production-scale multi-process/profile telemetry and configurable admission policy |
| Task Checkpoint/Resume | PARTIAL | migrations 11–15 and repository persist graph/input/hash/version, identity, current node plus exact scoped node key, completed scoped nodes, Tool results, variables, all node outputs, loop cursors, retry cursors, checkpoints, bounded evidence, Task Graph-owned waiting questions and timed deadlines, terminal result, permissions, limits, provenance and revision; every supported node/Tool/checkpoint/loop/parallel/question boundary writes state; `task.checkpoint` appends bounded external-Brain evidence only after execution reaches a persistent node boundary; safe pause and durable user answers resume from completed scoped node IDs, loop/retry cursors and immutable Tool results without repeating effects; startup preserves and schedules safe `WAITING` deadlines, preserves `PAUSED` executions and marks uncertain `READY`/`RUNNING` work `RECONCILIATION_REQUIRED`; explicit recovery first imports an exact durable Gateway result when available, otherwise resumes an unconfirmed idempotent Tool or reuses only a result whose execution/scoped-node/attempt/Tool identity matches, while non-idempotent unknown effects remain quarantined | repository optimistic-revision/restart/result tests, injected Tool transport failure and exact post-effect/pre-result repository failure, Runtime pause/resume tests at sequence, timed WAITING and in-loop Tool boundaries, nonblocking dual-wait worker release, timed and retry-backoff restart continuation, stable next-attempt call ID without replay, external checkpoint persistence, ASK_USER restart/resume, idempotent restart continuation, exact non-idempotent nested-loop result reuse and prior-iteration rejection, durable Minecraft task terminal import without Tool replay, Runtime replacement after injected result-persistence failure without Tool replay, abrupt live Runtime/Fabric process crash after durable Tool completion and before Graph result persistence followed by verified recovery without a second behavior run, request-hash/call/session/companion mismatch rejection, graph-hash/Evidence-shape rejection, duplicate-answer rejection and waiting-question cancellation | broader storage corruption/full-disk and process timing fault-injection breadth |
| Agent Workspace | PARTIAL | Runtime-owned logical resource store isolates hashed profile/companion scopes, exposes no host path, allows only `.yaml/.yml/.json/.md`, enforces 128-file/2 MiB/64 KiB quotas, rejects traversal/absolute/drive/backslash/ADS/reserved/control/non-NFC paths, verifies every existing path against symlink/reparse/scope escape, atomically replaces content/index, versions changed content, records SHA-256, retains at most eight content+checksum backups per resource, and restores retained content only as a new monotonic version; the Terminal exposes only scoped Skill draft metadata/content and integrity-checked retained versions rather than a general file browser | local workspace isolation/version/hash/quota/path attack tests; restore/list tests prove retention pruning, checksum tamper rejection, monotonic versioning and companion isolation; Runtime integration proves local scoped listing/restore; symlink escape test is present but explicitly skipped on this Windows host because the test process cannot create links | remote link/junction coverage and legacy checksum migration/export tooling |
| Built-in Skills | PARTIAL | read-only classpath catalog exposes six declarative Task Graph compatibility Skills for existing collect, mine, smelt, withdraw, craft and defend composites; IDs cannot be overwritten or promoted, `skill.execute` runs them through the persistent Task Graph Runtime rather than a new Handler, and the Terminal presents their format/hash/trust as read-only | all resources parse and validate against matching current Tool permissions; fake Tool integration proves one built-in persists and executes through Task Graph with built-in provenance; Runtime/Web tests cover catalog presentation | replace compatibility wrappers with primitive compositions as Primitive coverage lands and remote verification |
| Generated Skill Quarantine | PARTIAL | `skill.list/read/save_draft/restore_draft/validate/request_promotion/execute` operate only on logical Skill IDs; save and retained-draft restore remain quarantined, restore creates a new workspace version rather than rewriting history, validation uses the real bounded JSON/YAML codec plus current executable node, Tool permission and input-schema contracts, promotion requests persist Brain/controller provenance, permissions, validation evidence and SHA, and only the ACTIVE approved version may execute through Task Graph; `skill.request_trial/execute_trial` add a one-use 60-to-900-second exact-scope lease for validated drafts, restricted to current LOW-risk Tools, `READ_WORLD`/`MEMORY`/`CONTROL_TASK`, tighter Graph/resource ceilings, hash verification and persistent Task Graph execution; trials never create an approved version, cannot call `skill.*`, retain bounded evidence, support immediate local revocation and become non-resumable after restart; the Terminal displays scoped drafts, integrity-checked retained versions, promotion documents and document-free trial metadata without exposing host paths | local Skill gateway save/read/list/restore/isolation, valid/invalid graph, pending-review, pre-approval execution rejection, approved asynchronous execution and provenance tests; trial repository/Gateway tests prove exact scope, one-use consumption, expiry, low-risk policy, evidence, immediate running cancellation and restart interruption; Runtime management integration covers scoped catalog/draft/version/trial listing, restore and revoke; Web tests cover review/restore/trial-revoke dispatch and production build; root `check` passes at implementation commit `4676193` | untrusted-content scanning beyond graph/schema validation and remote verification |
| Skill Promotion | PARTIAL | migration 17 persists numbered review records and document content/hash; the Brain can only request promotion, while authenticated loopback management approves/rejects as `LOCAL_MANAGEMENT_USER`; approve/reject require the request's profile and companion to match the explicit management scope; approval supersedes the prior ACTIVE version, `skill.disable`/`skill.rollback` can only act on previously approved versions within the same profile/companion, and `skill.execute` loads only ACTIVE content with a fresh SHA check; active executions retain their exact approved request ID, disable cancels that Skill's running Graphs, rollback cancels only executions of the replaced version, and a post-start ACTIVE recheck closes the concurrent start/revocation race; the Terminal exposes explicit approve/reject/disable/rollback controls through its CSRF-protected proxy | repository approval/rejection boundary, dedupe, two-version supersession, disable, approved rollback and cross-companion/pending rejection tests; Runtime authenticated HTTP integration proves unauthorized and wrong-companion approval denial plus scoped approval; timed Graph tests prove disable and rollback cancellation, retained-version selection, and that revocation in companion A does not cancel the same Skill ID in companion B; Skills-page Vitest and production Web build pass | explicit configurable Promotion Policy, signature/administrator policy and remote verification |
| Registry/Mod Compatibility | LOCALLY_VERIFIED | Fabric 1.21.1 and Forge 1.20.1 advertise capability-gated live Registry/recipe/spatial/menu-observation queries; Runtime binds bounded results to query ID, authenticated Runtime session, companion and Brain call; ITEM/BLOCK/ENTITY/MENU and loaded DIMENSION discovery, crafting/smelting recipes, bounded tags/component type names/tool requirements/station requirements/slot roles, and visible block/item/entity/menu state use the connected server instead of per-Mod adapters; generic block/entity/menu actions require type-appropriate observed postconditions instead of trusting a vanilla return flag or one inventory/slot side effect; menu inspection declares slot/carried, synchronized data, state-ID and close postconditions; exact placed-world state is authoritative in Survival and Creative; unobserved effects return `UNCERTAIN_EFFECT` to the external Brain and the stopped non-idempotent action cannot resume/replay from its discarded progress | Fabric GameTests register the unknown `mcac_unknown_fixture` Item/Block/Entity/Recipe/Item+Block+Entity tags/component/container; Forge loads a separate GameTest-only unknown-namespace Mod and discovers its item, block tag and recipe through generic connected-server data while excluding it from the production JAR; Fabric real-server cases verify Creative placement with unchanged inventory, a synthetic Mod-style menu whose synchronized data changes while slot/carried/state ID do not, and an inert interaction that returns `UNCERTAIN_EFFECT` then remains unreplayed after explicit resume; `unknownModGenericE2E` starts a real Fabric ServerPlayer and a deterministic external client (`liveModel=false`) whose initial Graph contains no target Registry ID, composes search→recipe→material describe/inspect→block inspect/interact→menu inspect/quick-move/re-observe/close→inventory verification, and preserves the existing ASK_USER/restart/crash-reconciliation E2E | Live Brain execution remains credential-blocked; fixtures are not third-party-Mod or human-play evidence and prove bounded generic mechanisms, not arbitrary Mod compatibility; NeoForge expansion remains outside the Full-Bridge RC target |
| Primitive-based Representative E2E | LOCALLY_VERIFIED | The production Runtime/Fabric E2E submits six structurally distinct declarative graphs through authenticated MCP from isolated external-client Brain sessions: unknown Registry search→conditional→describe; typed position→live block Observation→`movement.look`; rejected schema→external correction→declared `fallback`; durable `ask_user`→normal user ingress→same-execution live branch; live inspect→timed WAITING→external pause→graceful Runtime replacement and Fabric reconnect→resume→primitive look without replaying the completed inspection; and live `movement.look` completion→held result boundary→abrupt Runtime kill→startup quarantine→exact durable result import | Repeated local runs pass all six graphs against real Fabric 1.21.1 state and require `LIVE_SERVER_REGISTRY` plus `VANILLA_ENTITY_LOOK` evidence; graceful restart evidence proves PAUSED before and after process replacement; abrupt-crash evidence proves the Minecraft Task is terminal while Graph Tool results are absent, then retains one command binding/behavior run and reaches Graph `SUCCEEDED`; `evidence/representative-task-graph.json` classifies all results `LOCAL_DETERMINISTIC_EXTERNAL_CLIENT_E2E`, `liveModel=false`, with the automated answer separately tagged `deterministicTestAnswer=true` | Remote verification of the six-graph SHA; then Live Brain external verification and human playtest, neither of which is claimed by this automation |
| Existing Composite Capability Migration | PARTIAL | Existing composites remain bounded compatibility entries; `movement.step/look/stop`, `block.break/interact/place`, `entity.collect/interact/attack`, and `inventory.transfer` now route into shared durable command/action executors rather than new Java scenario Handlers, while declarative built-in Skills remain available for larger compatibility flows | Runtime protocol-capture tests prove alias-to-shared-executor mapping with unknown namespaced content; real Fabric GameTests verify shared movement/rotation/mining/pickup/block placement/entity attack/container effects; direct Runtime/Fabric equivalence E2E now round-trips one item through generic `inventory.transfer`, consumes a scoped generic menu capability, and later completes the compatibility `inventory.withdraw` shortage chain against the same live chest | Migrate remaining composites onto completed primitive compositions or built-in Skills and add direct equivalence for the remaining aliases |
| External Brain Bridge | REMOTELY_VERIFIED | `runtime/.../brain`, authenticated `/brain`, game/Web ingress | PR fast, Windows, Minecraft heavy green at audited baseline `1a3fab4` | Live provider verification excluded |
| External Brain Semantic State | LOCALLY_VERIFIED | optional Hermes `mcac-brain/1` turn extension carries a strict versioned snapshot for conversation context, immediate instruction, current task, long-term goal, pause/interruption reason, user takeover, quiet/normal/active initiative, companion/immersive-roleplay personality, descriptive permission preset, explicit player-away state, latest real-world observation time, and stale assumptions; Runtime rejects unknown/oversized/invalid fields, never derives a competing state, never treats the descriptive preset as Tool authority, persists monotonic revisions under exact controller/session/Companion scope, restores the same session projection, injects it into the next bounded Hermes turn, and exposes it through authenticated audit and the Brain UI | Hermes bridge round-trip proves a newly authored state reaches the next call; malformed enum/schema rejection, migration-24 persistence/revision/scope rejection, bounded-context projection and Brain-page rendering tests pass | Live Hermes authorship and instruction/initiative/personality acceptance scenarios remain pending; OpenAI-compatible semantic-state authorship is not yet implemented |
| Instruction / Interruption Semantics | LOCALLY_VERIFIED | conservative ingress separates waiting answers, exact controls, explicit immediate movement instructions, goal modification and ordinary conversation without interpreting open-ended strategy; hypothetical/joke wording remains conversation; an immediate instruction requests safe pause without waiting for the old Companion turn lock, stops that old turn before another Tool call, and delivers its real PAUSED observation with the new message to the same external Brain; cancellation/goal replacement invalidates the prior Brain session so its goal cannot continue; Fabric and Forge emit scoped owner block-use/break activity, and Runtime yields only when its dimension and XYZ exactly match an active direct or Task-Graph child Tool target | classifier cases cover ordinary/hypothetical speech, exact follow, goal change and controls; coordinator concurrency tests prove nonblocking pause, PAUSED-observation handoff and no silent old-turn continuation/cross-target preemption; Runtime Tool tests prove recursive exact-target matching and nearby-target rejection; Forge Runtime E2E proves its authenticated owner-activity transmission; root `check` and all three Loader builds pass at `3d2f5ee` | Live Hermes and human interaction quality remain pending |
| Initiative / Personality Modes | LOCALLY_VERIFIED | migration 25 persists local-user-owned per-Companion initiative (`QUIET`/`NORMAL`/`ACTIVE`, default `NORMAL`) and personality (`COMPANION`/`IMMERSIVE_ROLEPLAY`, default `COMPANION`) settings; authenticated local management and the Brain page can view/change both; each bounded Brain turn receives the settings and a Brain semantic snapshot that conflicts with them is rejected; `conversation.propose_proactive` accepts Brain-authored speech only when it cites a meaningful terminal Tool observation from the exact Brain session; migration 29 atomically deduplicates each evidence event, bounds retention, links the durable conversation event, suppresses ordinary milestones in QUIET, and applies five-minute/one-minute/fifteen-second QUIET/NORMAL/ACTIVE intervals through the existing offline outbox; modes cannot change Tool permissions, safety, budgets, Memory policy or evidence requirements | repository tests prove defaults, revision, Companion isolation, atomic event dedupe, rate windows and retention bounds; coordinator tests prove settings reach successive turns, Tool definitions remain identical across a mode switch, and Brain override is rejected; proactive Gateway tests prove evidence binding, event significance, duplicate/rate suppression, QUIET milestone suppression and durable outbox delivery; authenticated Runtime HTTP integration, Brain-page Vitest, production Web build and root `check` pass at implementation commit `817f1fa` | Live Hermes and human behavior/usability verification |
| Final Observation / Completion Claim | LOCALLY_VERIFIED | every Hermes final response carries a structured `VERIFIED`, `UNVERIFIED`, or `NOT_APPLICABLE` claim; the external Brain chooses the observation Tool and, for `VERIFIED`, supplies one to sixteen bounded JSON-Pointer postconditions using `EQUALS`, `AT_LEAST`, `AT_MOST`, or `CONTAINS`; Runtime requires a successful terminal same-session observation, validates the optional task binding, deterministically evaluates every condition, and rejects missing fields, type mismatches or failed comparisons without interpreting natural language; migration 26 stores the audited Tool foreign key and migration 31 stores the typed conditions; authenticated audit/UI show both | Hermes adapter requires the structure; coordinator tests prove accepted exact-call linkage, missing-call rejection, honest unverified completion, and rejection when an unrelated successful observation lacks the claimed inventory condition; migration/table/audit inspection and Brain-page rendering tests pass | Live Hermes must demonstrate appropriate condition authorship and human reviewers must assess certainty wording; OpenAI-compatible completion-claim authorship is not yet implemented |
| Async Tool Loop | LOCALLY_VERIFIED | a durable action start immediately returns a terminal receipt for the request with `taskId` or `executionId`, `executionMode=ASYNCHRONOUS`, `completionVerified=false`, and the matching inspect Tool; request acknowledgement no longer waits for or cancels a healthy five-minute body action or persistent Task Graph; the action remains cancellable through its session-bound registration, while later `task.inspect`/`task_graph.inspect` provides real state; non-durable waits retain bounded timeout cancellation and unconfirmed cancellation still persists `RECONCILIATION_REQUIRED`; observed blocked lifecycle events retain their exact failure code and audited call-ID dedupe remains enforced | coordinator tests prove durable start returns without `awaitTerminal`, carries an explicit non-completion receipt and remains explicitly cancellable; Gateway tests verify short acknowledgement/control deadlines versus five-minute body deadlines; PAUSED/reconciliation/confirmed-timeout/unconfirmed-timeout, duplicate-call, concurrent-cancel, companion-isolation and restart tests remain green | Remote verification of the corrective slice and production-scale long-action telemetry |
| Hermes Resume | LOCALLY_VERIFIED | same-session resume, durable question recovery and once-only undelivered observation delivery; live adapters now enforce bounded exponential backoff with jitter, aggregate request/input/output/wall/retry budgets, emit `RECONNECTING/SAFE_IDLE`, request the existing idempotent `movement.stop` safety primitive, preserve companion/session identity, and let user cancel or Runtime close interrupt the wait immediately; bounded context assembly keeps recent conversation/current task/approved memory/verified world data in per-category and total character budgets, deduplicates repeated entries, clips oversized Tool observations, and records only clipping statistics | coordinator interruption/resume and audit restart tests; `brainReconnectE2E` proves two network failures, SAFE_IDLE notification, same-session recovery, cancellation during backoff and another companion session remaining usable; Runtime module tests cover context clipping and existing ASK_USER/restart behavior | Real Hermes/provider transport remains credential-blocked; longer reconnect soak and human verification remain in the v4 sprint |
| External ASK_USER | LOCALLY_VERIFIED | structured Hermes/OpenAI-compatible question; durable brainSession/task binding; Web/game routing; idempotent answer | adapter/restart/HTTP/game Replay plus real Fabric `6/16 -> ASK -> withdraw 6 -> return -> deliver` E2E | Remote verification of current SHA; Live/Human excluded |
| Search | PARTIAL | bounded query/open/citations/cancel gateway and privacy filtering; five-minute/128-entry in-memory provider cache is companion+policy scoped and clears the relevant entry on cancel; migration 21 persists at most 256 KiB of bounded query policy plus untrusted source metadata under the exact controller + Brain session + companion scope, restores source-ID authorization across Runtime restart, physically deletes replaced/cancelled/expired rows, caps each companion at 128 rows and never persists opened page content into Memory; the real HTTP provider resolves and validates every target before connecting, rejects any mixed public/non-public answer across IPv4/IPv6 loopback, link-local, private, ULA, CGNAT, documentation/reserved and multicast ranges, pins the socket to the validated address set while retaining TLS SNI/hostname verification, re-resolves and revalidates every content redirect, caps redirects at four, rejects loops/compression/ambiguous framing and never forwards the Provider bearer credential across a redirect; a pairing-authenticated read-only Runtime endpoint feeds the existing Search & Privacy page with source metadata labelled `UNTRUSTED_EXTERNAL_CONTENT`, and external HTTPS links open only on explicit user click with opener/referrer isolation; Terminal/Web configuration and Doctor remain bounded and credential-free | Runtime Replay and cache-isolation/cancel tests; repository/Gateway restart tests cover exact-scope source recovery, supersession, physical cancel/expiry, size/capacity bounds and cross-controller/companion rejection; address-class and mixed-answer unit tests, pinned-address and per-hop redirect tests, simulated DNS rebinding rejection before the second connection, a real loopback-socket Provider integration that proves the configured Host plus denial of credential-bearing redirects, and a hostile-page test that flags prompt injection while proving it cannot add Tool authority or enter citations; Runtime integration verifies the UI endpoint exposes source metadata but not opened page content; Search page Vitest checks safe external-link attributes; production Web build passes | remote/live provider verification and broader user study of source review UX |
| Unknown Mod + Search Safety | LOCALLY_VERIFIED | unknown Registry content remains data, not executable policy: both Full-Bridge Loaders use generic connected-body Registry/tag/recipe mechanisms, Search bodies remain labelled untrusted, and unsupported generic interaction is returned honestly without inventing an adapter, Handler, mutation, or success | Fabric uses an isolated unknown-namespace fixture in its real-server compatibility suite; Forge adds a separate GameTest-only Mod whose unknown item, block tag and recipe are discovered on a real Java 17 server, all 4/4 Forge tests pass, and the production JAR excludes the fixture; Runtime hostile-page coverage proves shell/filesystem/Memory-approval/world-edit authority cannot be expanded, while a deterministic external client limited to `registry.describe` and `recipe.query` ends with `UNSUPPORTED_GENERIC_INTERACTION` and zero world/menu/Memory mutation | Live Hermes must demonstrate appropriate generic discovery and honest unsupported behavior against user-selected third-party Mods; human review remains `HUMAN_PLAYTEST_PENDING` |
| Memory | LOCALLY_VERIFIED | four typed fact categories remain separate from migration 23's bounded deterministic `episode_capsule` store; candidates remain exact-companion/session bound, capped/expiring in quarantine, show Capsule provenance and same-key conflicts, and only authenticated local management may approve/reject; migration 27 adds per-Companion automatic-save settings and durable prior versions; authenticated management and the Brain page provide view/search/filter/edit/delete/category clear, Profile/world plus Companion scope, provenance/update time/history, and aggregate-only safe export; common credential/email material and explicit full-chat/full-Prompt/Search-body sources are rejected centrally; the external Brain may save only a bounded `PREFERENCE` through `memory.remember_explicit_preference`, while Runtime requires enabled automatic save and an exact, fresh latest-owner-message match in the same Companion and stores `USER_EXPLICIT_BRAIN_CAPTURE` provenance | Capsule/candidate suites retain deterministic projection, evidence, isolation, quarantine/conflict and review coverage; repository tests prove automatic-save, edit/delete history, value-free export, and same-Companion isolation across separate Profile/world databases; Tool tests prove explicit preference save plus mismatched/cross-Companion evidence and paused-setting rejection; authenticated Runtime HTTP, Brain-page Vitest, production Web build, and support-bundle body-exclusion pass; root `check` passes | Live Hermes must demonstrate appropriate stable/low-risk preference judgment and human reviewers must verify interaction quality; production-duration history retention tuning remains post-RC hardening |
| LocateKnownContainer | LOCALLY_VERIFIED | bounded `world.locate_known_container` returns only body/user-verified World Memory and flags cross-dimension candidates | multi-container, cross-dimension, unverified-filter Runtime tests | remote verification of current SHA |
| Container | LOCALLY_VERIFIED | vanilla withdraw/deposit, verified deltas, and known-container lookup | Fabric GameTests and Runtime/Fabric E2E | multi-container fill, invalidation/restart breadth |
| Craft | LOCALLY_VERIFIED | vanilla 2x2/3x3 recipe/menu execution on both Full-Bridge Loaders, selected from each target version's live RecipeManager without scenario handlers | Fabric GameTests; real Forge 1.20.1 GameTest independently verifies one log to four planks through the inventory grid and eight planks to one chest through a live crafting-table menu, including exact material deltas | broader shaped/shapeless/remaining-item recipes, cancellation and restart cases |
| Explore | PARTIAL | `world.scan` dispatches incremental `ExploreArea` on Fabric and Forge: radius 1..16, 256 blocks/tick, loaded chunks only, 64 candidates, distance ranking and lifecycle-bound candidate evidence | Runtime schema test; real Fabric GameTest proves asynchronous scan and ranked ore candidates; real Forge 1.20.1 GameTest independently verifies two live gold-ore candidates and distance ordering | route exploration, world-change/unreachable/restart breadth, dual-loader Brain E2E |
| Collect | PARTIAL | `resource.collect` uses walking input plus vanilla `ItemEntity.playerTouch`; validates inventory delta, missing/insufficient/full/no-progress states | Runtime schema test; isolated real Fabric GameTest uses a bounded support surface, collects a coal stack after movement, and proves entity removal + inventory delta; full cross-loader GameTest passes | scattered/moving drops, restart/unreachable breadth, wood/iron/diamond cases, Brain E2E |
| Mine | PARTIAL | `resource.mine_vein` dispatches bounded `MineResourceVein`; discovers a connected six-neighbor vein, accumulates vanilla hardness progress, snapshots nearby item entities immediately before each break, breaks through `ServerPlayerGameMode`, claims only newly spawned drop UUIDs, and verifies per-item body inventory deltas before accepting completion | Runtime schema test; isolated real Fabric GameTest mines two supported connected diamond ores, verifies both world changes, two vanilla drops, iron-pickaxe durability >=2, and audited action-path evidence; the 23-test Fabric suite passed repeatedly after unrelated concurrent item entities were excluded from drop ownership; full cross-loader GameTest passes | navigation to distant veins, mixed/common resources, tool break/full inventory/world change/restart breadth, Brain E2E |
| Smelt | PARTIAL | `item.smelt` resolves a target-version smelting recipe, requires an empty verified furnace in reach, transfers input/fuel through `FurnaceMenu`, waits for the real cook lifecycle, retrieves the result through the menu, and verifies the output inventory delta on Fabric and Forge | Runtime schema boundary test; real Fabric GameTest smelts two raw iron with coal; real Forge 1.20.1 GameTest independently smelts raw iron to an ingot and verifies input consumption, result pickup and cleared input/result slots | fuel-duration preflight, alternate furnace/recipe/fuel breadth, busy/full/fuel-loss GameTests, cancellation/restart and dual-loader Brain E2E |
| Retreat | PARTIAL | Runtime-independent pre-task reflex detects a live hostile within 3 blocks, interrupts any active navigation/skill through the normal action gateway, walks directly away with player input, requires >=3-block displacement and >=6-block threat clearance, then leaves the interrupted work explicitly PAUSED; capability-gated `safety.retreat` accepts one external Brain-selected visible live entity within 16 blocks and runs the same bounded executor to an IDLE terminal result on Fabric and Forge | isolated real Fabric tests prove both reflex interruption and an explicit UUID-bound retreat; the real Forge 1.20.1 GameTest independently verifies UUID binding, ordinary player travel, three-block displacement and six-block clearance; Loader suites pass | terrain-aware escape planning, fire/lava/drowning directional recovery, multiple/ranged threats, restart and dual-loader Brain E2E |
| Defend | PARTIAL | `combat.defend_owner` is exposed through both Full-Bridge Loader capability intersections; each bounded executor selects one live hostile within 8 blocks of the owner, approaches with player input, honors vanilla attack cooldown, attacks through `ServerPlayer.attack`, and completes only when the threat dies or remains outside the 10-block protection radius | Runtime zero-argument schema test; isolated real Fabric combat GameTest kills a one-health husk and verifies advancement/durability/evidence; the real Forge 1.20.1 GameTest independently kills a one-health zombie through the vanilla ServerPlayer combat path; Loader suites pass | hostile intent/ownership policy, multiple/ranged threats, armor/weapon strategy, friendly-fire breadth, cancellation/restart and dual-loader Brain E2E |
| Multi-strategy | PARTIAL | External Brain can chain verified tools | container chain Replay E2E | route/resource/tool/fuel/diamond strategy E2E |
| MCP / Tool Protocol | PARTIAL | authenticated `/mcp` Streamable HTTP JSON-RPC endpoint negotiates MCP `2025-03-26`/`2025-06-18`, enforces the negotiated version, exposes only context-filtered MCAC Tool Gateway definitions, streams token-bound `notifications/progress`, cancels disconnected streams, and returns structured terminal observations; the management server uses a fair bounded 16-thread/64-request queue and a separate fair bounded two-thread/16-request planning queue, while synchronous and streaming MCP work are independently capped at 8 and 4 so at least four management threads remain available for health, cancellation, and control; category saturation returns `RUNTIME_BUSY`, and shutdown interrupts then confirms both pools terminate; every Runtime JSON entry reads through an eight-reader, five-second pre-parse boundary with 16-KiB Brain, 128-KiB ordinary-management, and 1-MiB MCP limits, rejects compression and ambiguous framing, validates declared length, counts actual chunked bytes, returns structured size/framing errors, and closes a body that misses its deadline without parsing or retaining it; migration 18 adds a fail-closed durable request ledger that binds the scoped call ID to Tool plus canonical arguments before dispatch, returns an exact persisted terminal result without redispatch, rejects concurrent/changed reuse, and quarantines restart-interrupted calls for reconciliation; migration 19 adds standard random 256-bit `Mcp-Session-Id` issuance, hash-only durable storage, exact controller + Brain session + companion + protocol binding, eight-hour absolute expiry, restart retention and authenticated DELETE termination; migration 20 persists SSE envelopes before transmission with opaque event IDs, same-session `GET /mcp` + `Last-Event-ID` ordered catch-up, 256-event/64-KiB bounds and inactive-session pruning; internal call IDs include the session hash so JSON-RPC IDs may be reused in a newly initialized session without aliasing old results; `docs/MCP_PROTOCOL.md` covers lifecycle and Hermes connection contract | Runtime HTTP integration verifies auth, negotiation/version rejection, bounded identity headers, random session issuance, required bearer, cross-companion rejection, termination, cross-session request-ID separation, bounded tool list, ID-bearing JSON/SSE terminal responses, ordered cursor catch-up, exact terminal replay, changed-argument conflict rejection and cancellation notification, and health availability while a Brain request is blocked; body-boundary integration covers declared oversize, actual chunked oversize, rejected compression, short/mismatched Content-Length, slow-body deadline cleanup, and immediate post-attack health; the management-pool test proves bounded rejection and the configured MCP category limits reserve control capacity; repository restart tests verify session hashing/scope/version/expiry/termination, event ordering/isolation/size/retention/pruning, request in-flight quarantine, terminal retention and Tool/argument conflicts; protocol unit test verifies progress envelope/token; Terminal Doctor performs the real session handshake before bounded tools/list; packaged multi-profile telemetry and the six-Graph Runtime/Fabric crash E2E pass through real initialization/session handling | separately provisioned long-term caller identity, explicit request nonce beyond session-scoped JSON-RPC ID, long-lived future-event GET attachment, live concurrent cancellation E2E, and verified Hermes-native configuration |
| Web UI | PARTIAL | existing chat/audit/Memory/Search/Task Graph/Skill controls are reused; the Brain page shows provider/reconnect state, aggregate context budgets, external-Brain semantic state and completion evidence, local initiative/personality selectors, Capsule/candidate provenance and conflicts, plus formal Memory search/filter/edit/delete/category clear, automatic-save control, scope/provenance/update/history, and aggregate-only export; Capsules, candidates and formal Memory remain separate; credentials, prompts, full Graphs/Tool logs/Search pages and raw Capsule bodies remain omitted; every Terminal JSON route retains bounded pre-parse controls and browser bootstrap tickets remain hash-only, bound, short-lived, one-use and capped; starting or reusing the HTML Terminal no longer invokes the Windows default-browser URI handler unless the operator explicitly supplies `--open-browser` or `MCAC_OPEN_BROWSER=true`, while `MCAC_NO_BROWSER=true` is an unconditional safety veto | Brain-page Vitest covers audit/semantic/completion/modes, Capsule/conflict/review, Memory history, automatic-save and export dispatch; authenticated Runtime/Terminal integration and support-bundle privacy tests pass; deterministic option tests cover safe default startup, CLI/environment opt-in, CLI opt-out and the unconditional no-browser veto; all 12 Web test files / 18 tests, lint and the TypeScript production build pass; bilingual Chromium automation against the real packaged Java backend covers every navigation route, launcher rescan, Brain configure/protocol-probe/disable, all seven Companion controls through an authenticated heartbeat-maintained Fabric protocol fixture, Task Graph pause/resume/cancel through real MCP and Runtime management, every currently reachable safe Doctor repair route under injected stopped-runtime/token/hash failures, and the existing install, Runtime, compatibility, support and uninstall controls | the external Brain endpoint and Companion body in this browser gate are controlled local protocol fixtures, not Live-provider or human-play evidence; real-provider and human-play usability remain external; proactive-message policy remains incomplete |
| Game UX | PARTIAL | natural reply outbox, durable ASK_USER routing, local pause/resume/cancel and coarse lifecycle notices remain; the Live Brain/human playtest guide provides the shortest Fabric-or-Forge SHA/install/Doctor/start/three-scenario/Capsule/review/Support-Bundle flow and a result form covering Loader, Tools, interventions, errors, repeats, danger, conversation, cost, generic gaps and subjective companion quality; the HTML terminal exposes Full Bridge controls for Fabric 1.21.1 and Forge 1.20.1 while retaining NeoForge `LOCAL_ONLY` | Runtime lifecycle/WebSocket/Fabric conversation tests, Terminal Full-Bridge support matrix tests, frontend component/build checks and documentation command/link checks; the guide explicitly forbids claiming Replay/Fixture/105-turn/soak as human or Live evidence | `HUMAN_PLAYTEST_PENDING`; no human result is fabricated |
| Reliability | PARTIAL | WAL, leases, restart interruption and dedupe; both Full-Bridge Loaders persist the last accepted control epoch plus behavior ID/revision, boot unfinished work into safe `PAUSED`, reject replayed epochs, and reconcile only through higher-epoch explicit resume; Runtime absorbs the authenticated Mod epoch floor and revokes any lower local authority; Brain transport has bounded retry/backoff/jitter, cancellation and SAFE_IDLE; Task Graph waits/retries persist deadlines and release workers; confirmed effects use exact stable identities while uncertain effects remain in reconciliation; `longPlay100TurnTest` composes these boundaries through 105 deterministic external turns, ASK_USER, reconnect, Runtime session interruption/resume, Graph pause/resume, user cancel, Capsule and reviewed Memory; `realPlaySoakTest` implements the 200-turn-first bounded local soak | LeaseService tests prove remote-floor monotonicity and stale local revocation; Fabric 31/31 and Forge 4/4 GameTests prove Loader behavior and persistence boundaries; separate real Fabric and Forge two-process server gates preserve the world and prove UUID/body/inventory recovery plus safe `PAUSED` quarantine of interrupted navigation; full Runtime/Fabric E2E proves Runtime restart, abrupt Tool-result crash recovery without replay, and an evidence-bound Replay Brain final claim after a new connected-body inventory observation; Runtime/Forge E2E proves authenticated controls when the Mod epoch is ahead; the 2026-07-22 non-cached `liveModel=false` long-play report records 104 unique Tool calls, 20 mutations, zero duplicate effects, DB 397,312 bytes, threads 16→16 and clean workers; the soak report records 200 turns/15.610s, heap 17,262,536→15,379,248 bytes (31,577,464 peak), threads 16→16, DB 425,984 bytes, WAL 0, average 75.37ms/p95 84.16ms, one reconnect, zero duplicate/reconciliation and zero sessions/connections/queue/workers after cleanup | broader dual-loader fault injection remains; live-provider disconnect evidence is `BLOCKED_BY_CREDENTIALS`; deterministic regression/soak do not prove model intelligence or human play quality |
| Task Graph Telemetry | LOCALLY_VERIFIED | authenticated `/health` reports aggregate-only Runtime status, active execution/Tool/timed-wait counts, Graph and parallel worker pool activity/queue depth and limits, three admission limits, and durable per-state/total execution counts; it exposes no execution, Brain-session or companion identifiers and degrades explicitly if durable aggregation is unavailable | the 64-execution saturation test asserts two active workers/Tools and 62 fair bounded queued executions distributed across four sessions/companions, rejects the seventeenth same-session and sixty-fifth global request, then proves zero live work and 64 durable successes; packaged dual-profile test executes one persistent timed Graph per isolated Runtime, observes one profile-local `WAITING` count on each, verifies independent `SUCCEEDED` totals, and proves stopping profile A does not change profile B telemetry | production-duration multi-profile load, retention/cardinality policy, leak/resource trend and remote verification |
| Installer | LOCALLY_VERIFIED | Windows installer components plus the existing HTML confirm/execute flow perform bounded launcher discovery, compatibility diagnosis, manifest-backed installation and verification; automatic launcher discovery applies its eight-second/60,000-entry budget to both directories and ordinary/failed file visits and terminates from the visitor callback at either boundary; update journals snapshot the prior manifest as well as replaced artifacts, and startup recovery always rolls an unfinished journal back instead of accepting an old-manifest/new-artifact mixed version; the Terminal operation layer serializes install/repair/update and Runtime operations for the same instance while allowing different instances to proceed independently; Runtime start/stop additionally use a fair in-process lock plus an OS file lock with a ten-second timeout and PID/operation owner metadata, so stale metadata after a crash cannot grant authority without the OS lock | deterministic budget tests cover exact entry exhaustion and an expired deadline; installer tests inject failures after backup, artifact install and manifest replacement and restore the prior artifact/byte-identical manifest; Runtime supervisor tests prove same-Profile exclusion, timeout/owner visibility, post-release recovery, and different-Profile independence; Terminal operation tests prove same-instance serialization across categories; the current portable ZIP is expanded into a clean directory and its packaged `mcac.exe` Playwright flow passes a real `/api/discovery/rescan` against the isolated PCL2 fixture, install plan/confirmation, Doctor, Runtime start, session attach, support bundle, rollback, reinstall, both uninstall policies and shutdown; package structure/hash verification and arbitrary-working-directory HTML first start also pass | discovery remains synchronous in the request, but the bounded callback termination and visible rescan loading state cap and expose that work; real personal-launcher step is `HUMAN_PLAYTEST_PENDING`; cross-process install-vs-Runtime coordination outside the managed Terminal remains to be hardened |
| Migration | LOCALLY_VERIFIED | versioned SQLite migrations through migration 30 cover durable Task Graph outputs/questions, Episode Capsules, Memory suggestion review/provenance, per-Companion Memory settings/prior versions, Skill review/version state and single-use trial leases, proactive-message admission/deduplication, MCP request/session/SSE ledgers with pairing-generation invalidation, Search sessions, session-scoped external-Brain semantic state, local behavior settings, and completion-claim/final-observation links; managed updates replace only manifest-owned artifacts and preserve the separate profile database, Workspace, identity and credential references | migration rollback/idempotency plus Memory settings/history, Skill trial schema/restart recovery, proactive-message rate/dedupe persistence, repository, semantic-state revision/isolation, behavior-setting default/revision/isolation, completion-claim linkage, and Capsule idempotency/restart tests; the installer lifecycle writes data/config/Workspace/identity sentinels before v1→v2, verifies them after success and injected rollback, and the packaged Runtime persistence gate recovers the same Companion UUID/body/inventory after restart | cross-version field migration observation is `POST_RC_HARDENING_DEFERRED` |
| Upgrade | LOCALLY_VERIFIED | update transaction backs up the prior managed artifact, snapshots the prior manifest, atomically replaces the artifact and preserves instance config, database, Workspace and identity data | a real temporary-filesystem v1-to-v2 lifecycle verifies the new artifact/manifest and all user-data sentinels while retaining unrelated worlds and Mods | final packaged release-to-release smoke and remote CI |
| Rollback | LOCALLY_VERIFIED | failed updates restore both the prior managed artifact and the exact prior manifest; a public recovery entry point resolves an unfinished journal after process restart | IOException injection after manifest write and an Error-style abrupt-crash simulation both restore v2 from a failed v3 update without accepting a mixed version | final packaged release-to-release smoke and remote CI |
| Uninstall | LOCALLY_VERIFIED | the control plane exposes separate preserve-data and delete-MCAC-data plans; both stop the selected Runtime before manifest/hash-bound removal, while delete mode is bounded to the instance `.mccompanion`, `config/minecraft-ai-companion`, and exact control profile | installer and terminal integration tests prove preserve versus delete semantics and retain worlds and unrelated Mods; Web unit/build checks cover the two explicit actions; the clean extracted-package Golden Path starts a real Runtime, performs preserve-data uninstall, verifies the process and both service ports are offline while the profile remains, reinstalls, then performs delete-data uninstall and verifies the profile is gone | remote CI and real personal-launcher human step |
| Doctor | LOCALLY_VERIFIED | existing launcher/install/runtime/token/protocol/Provider/Search checks are reused; the live MCP probe now separately reports current-scope generic Registry discovery, Episode Capsule reads and Memory candidate submission while confirming no Brain review Tool; Doctor also reports credential presence without value/storage, numeric Live validation budgets, and accepts Live evidence only from a sanitized profile report with `liveModel=true` and exactly three PASS scenarios | MCP fake server verifies negotiation, forbidden-tool rejection and all three generic validation surfaces; aggregate Doctor tests retain Provider/Search behavior; real-backend Chromium automation injects stopped Runtime/health/protocol, mismatched pairing-token and managed-artifact hash failures and clicks the advertised restart, rotate-token and install-repair buttons to successful completion; Runtime exposes aggregate context budgets only and Web/secret/docs checks pass | actual credential/provider/scenario evidence remains `BLOCKED_BY_CREDENTIALS` until an external run writes valid sanitized evidence |
| Support Bundle | LOCALLY_VERIFIED | the allow-listed archive contains static diagnostics, current Doctor results, safe Brain/Search configuration presence (never values), managed manifest when present, error-only bounded Minecraft/Runtime excerpts, a non-identifying Runtime summary, reproduction steps and Mod filenames; raw chat and non-error log lines are excluded; every entry is redacted before write and the completed ZIP is scanned again for credential assignments, bearer/query tokens, passwords, account/player identifiers, email, JWT, Windows/POSIX paths, IP, UUID and hostname patterns | adversarial bundle test injects plain, JSON and query-string secrets plus account, email, JWT, both path forms, IPv4/IPv6, UUID, hostname and raw private-chat sentinels into both source logs and Doctor evidence, verifies their absence while preserving reason codes, and asserts the exact archive entry allow-list | remote CI and human review of a real failure bundle |
| Release Package | REMOTELY_VERIFIED | the portable root contains launchers, bundled JRE/app/runtime/Web UI, three Loader artifacts, legal/user/architecture/post-productization-P0 documents, known limitations, a file-level release manifest bound to the source commit, an SPDX 2.3 SBOM for every packaged JAR and every production npm component bundled into the Web assets, complete payload SHA256SUMS and an external ZIP SHA-256 sidecar; SBOM generation reads embedded Maven coordinates/versions or signed Manifest versions instead of applying the MCAC product version globally, identifies MCAC, Maven, npm and OpenJDK components separately, records declared licenses, supplier where known, purl, available download source and exact JAR SHA-256 or npm lockfile SHA-512, and uses the source commit timestamp for deterministic creation metadata; the merged jpackage input contains one diagnostic SLF4J provider rather than a competing NOP provider | `verifyTerminalPackage` requires exact SBOM-to-JAR set equality, the exact four production npm bundle components, source-SHA namespace binding, nonempty real versions/licenses/purls, lockfile checksums, the known dependency version map, product-component versions and explicit rejection of third-party versions overwritten by the product version; the stage-1 rebuilt 31-package release contains 27 JAR plus four npm components and passes with actual versions including Jackson 2.18.3, jsoup 1.18.3, picocli 4.7.7, SQLite JDBC 3.49.1.0, OpenJDK jrt-fs 21.0.11, React 19.2.7 and lucide-react 1.24.0; Windows run `30250337599` at closure checkpoint `7fbc47d7595f3fbab6ddbea2ed35bbaa56669e5f` passes package verification, clean-extraction Golden Path and artifact upload | Live Brain and human play remain external |
| Stage 6 Automation/Release Closure | REMOTELY_VERIFIED | Minecraft-heavy now has unconditional Fabric and Forge Runtime E2E plus two-process restart gates, with no `continue-on-error` or default-branch skip; the Windows chain packages all three Loader artifacts while installation remains exact selected-instance/Loader scoped; Forge's extended lifecycle navigation arena is moved into a separate GameTest corridor before construction so adjacent templates cannot contaminate its water/hazard phases, while bounded waits retain the exact door, three-block retreat and six-block clearance acceptance conditions; Runtime/Forge E2E allows 180 seconds for a cold hosted runner to reach its post-primitive verified marker but still requires that marker, an authenticated online body, FOLLOW/pause/resume/cancel and a successful GameTest exit | the complete local chain at source `0469a179111b2202242493e001ea45575e50a773` passes `clean check`, three Loader builds/launches, Fabric 31/31, Forge 4/4 and NeoForge 1/1 GameTests, dual Full-Bridge Runtime E2E, dual restart recovery, crash/no-replay reconciliation, unknown-Mod generic E2E, reconnect, 105-turn/200-turn reliability runs, Web unit/build/Chromium E2E, multi-Profile, disabled Runtime, package/SBOM/manifest/hash checks and clean-extraction install/update/rollback/two-uninstall Golden Path; after hosted run `30247592089` exposed cross-template lava contamination, corrected test source `f57a95fd851d30da4896aa84254c8c6f8e1bf429` passes the full Forge 4/4 suite twice consecutively with unchanged safety/mutation assertions; hosted run `30249333781` then passes all Loader GameTests and exposes only the prior 90-second Runtime/Forge cold-start allowance, whose corrected source `8f656c9867c7c0e7b1ae81dbf6818e01ba957c2e` passes focused Runtime/Forge E2E locally; final checkpoint `7fbc47d7595f3fbab6ddbea2ed35bbaa56669e5f` passes PR fast `30250337641`, Windows package `30250337599`, dependency review and both CodeQL analyses `30250337611`, plus Minecraft-heavy `30250341444` with every Loader build/launch/GameTest, both Runtime E2E paths and both restart-recovery gates; npm audit reports zero vulnerabilities; the 401-file/31-package candidate ZIP SHA-256 is `acd85f451f039268deb4c74b9a107ce1d1369cd8e0e89a5a51c97d24f78bf4b2` | Live Hermes and human play are explicitly deferred by the user and remain `LIVE_BRAIN_EXTERNAL_VERIFICATION_PENDING` / `HUMAN_PLAYTEST_PENDING` |
| CI | DEFINED_PENDING_REMOTE | validation is limited to three distinct boundaries: Layer 1 runs shared checks plus change-scoped secret/dependency review on PR updates; Layer 2 runs relevant Windows integration once when a coherent PR is opened non-Draft or moved to Ready; Layer 3 runs Heavy Loader/Runtime, full security analysis and Gradle compatibility on a schedule or manual dispatch. Ordinary `main` pushes do not duplicate PR validation. Only manual Layer 3 `candidate` mode from exact `main`, after every audit job passes, may build and upload an RC. Workflow actions remain pinned, npm remains lockfile-driven, and dependency verification/wrapper checks remain product supply-chain controls rather than separate delivery ceremonies. | static and focused local validation covers the new workflow syntax, three-file boundary, trigger separation, pinned actions, documentation contract and removal of per-SHA candidate uploads; the historical four-workflow runs above remain evidence only for their named revisions and do not verify this new trigger model | first natural remote execution of each applicable layer; Live/Human evidence is intentionally excluded from CI |

| Local Credential and Bootstrap Boundary | LOCALLY_VERIFIED | Runtime, Mod pairing and Web Terminal state files use atomic replacement followed by fail-closed owner-only verification: MCAC-created files are atomically created empty, assigned to the resolved individual current-user principal on Windows and restricted before data is written, while existing foreign-owned files remain rejected; POSIX uses mode `0600`; migration 30 binds every MCP session to a domain-separated pairing-token-generation hash, preserving same-token restart recovery while rejecting every pre-rotation session through the existing nondifferentiating not-found response; bootstrap state contains only a short-lived one-use URL and is deleted at shutdown | owner-bound creation/non-overwrite tests, pairing create/reuse and malformed-state tests, successful token rotation plus both rollback paths, same-token repository restart, changed-token session rejection, cross-Companion/session denial, Web bootstrap ACL/lifecycle tests, and a Windows real-backend Chromium flow after the hosted elevated-owner failure | independent Controller PKI and complete user-to-Companion authorization remain `PARTIAL` and are a separate P0 architecture item |
| Link/Reparse and Game Localization Hardening | LOCALLY_VERIFIED | Workspace resources and retained backups reject linked files/directories and post-validation replacement; installer managed, backup and restore paths reject symlink/junction/reparse components; Compatibility Store revalidates every fixed internal directory before and after locked operations; en-US and zh-CN Minecraft resources supply command help/input errors/chat prefix while technical codes remain stable; the former internal `/agent`, CLI ask planner and no-Brain game-chat fallback no longer have production decision authority | real local Windows junction tests cover Workspace, installer/backup and Compatibility Store escape with untouched outside targets; Linux unit execution creates real file/directory symlinks; Windows CI forces the same Workspace suite with `MCAC_REQUIRE_WINDOWS_LINK_TESTS=1`, making missing file/dir symlink capability an explicit `BLOCKED_BY_RUNNER_CAPABILITY` failure; localization key parity/source reference checks and all three 0.3.1 Loader builds prove both language files are packaged | exact Windows hosted file/directory symlink evidence remains pending the repair PR; broader translation of arbitrary external-Brain text is neither possible nor claimed |
| Product Limit and Adapter Audit | LOCALLY_VERIFIED | the Live guide uses real current CLI operations, applies independent Brain settings through a bounded Runtime restart, preserves result/budget/stop/support-bundle controls, and Doctor accepts Live evidence only for `liveModel=true` plus exactly three PASS scenarios; a validated machine-readable matrix distinguishes Hermes, OpenAI-compatible and Replay Tool/semantic/completion/ASK/resume/initiative/MCP/Live capabilities; navigation route construction, the first real third-party Compatibility Pack and additional survival Skills have explicit external-Brain-authority acceptance entries instead of fabricated implementation claims | Runtime profile tests prove `brain.json` emits the active Hermes/OpenAI-compatible block independently of legacy Provider configuration, persists all request/input/output/wall-clock/retry/Tool budgets without storing a token, and honors bounded timeout; the live probe requires a real MCAC Tool call and reports `SESSION_CLEANUP_FAILED` when cancellation of an opened Hermes health session is rejected or cannot be completed instead of silently accepting leaked probe state; documentation validation enforces the exact Adapter set and honest unsupported/pending states; strict Compatibility Host tests cover the schema used by the real-pack template; `BuiltinSkillCatalogTest` parses, validates and executes the exact six declarative Skills against current Tool aliases and permissions | `LIVE_BRAIN_EXTERNAL_VERIFICATION_PENDING`; `HUMAN_PLAYTEST_PENDING`; active block-breaking/placing route construction, a real third-party pack, Hermes-native MCP configuration and broader survival content are documented future acceptance work, not 0.3.1 PASS claims |
| 0.3.1 Candidate Regression Gate | LOCALLY_VERIFIED | Forge retreat acceptance measures its unchanged 480-tick budget against actual server ticks; Fabric and Forge stateful fixtures declare isolated forced arenas and entity/player ownership; chained behavior completion remains non-terminal until the final boundary; Fabric Runtime E2E keeps `/agent` closed with 410 and uses only the separate Replay external-Brain path; release staging and child diagnostics use Provider-owned output; Windows installer journals close and force same-directory temporary files before bounded replacement | frozen implementation candidate `8aa284f3f44c8728658232a3cd01d75acd4ec900` passes clean shared checks and three Loader builds/launches; Fabric 31/31 with exactly 30 Companions created/removed, Forge 4/4 and NeoForge 1/1; authenticated Fabric/Forge Runtime E2E; Fabric/Forge restart recovery; multi-Profile, Runtime-disabled, Brain reconnect, 100-turn, soak, package, arbitrary-directory HTML start, clean-extraction Golden Path, warning-all check; and Web exact install, zero-vulnerability production audit, lint, 15/15 unit tests, build and real-backend E2E. Release manifest sourceCommit equals the candidate; the SHA-bound ignored ledger preserves every gate and the one externally interrupted process plus its single permitted successful recovery | exact closeout-SHA remote CI, ordinary merge, exact-main rebuild/CI, annotated tag and GitHub Release remain Phase 9; Live Brain and human play remain pending |
| 0.3.1 Forge Staged Lifecycle Isolation | LOCALLY_VERIFIED | the Forge 4/4 lifecycle acceptance no longer accumulates retreat, door/obstacle, stairs, water, climbing and hazard/follow movement beyond one small structure: every long movement stage starts in its own forced, sealed generated-world-independent arena, resets only prior-fixture velocity/fall/air/fire residue, and retains the original materials, targets, tick budgets, movement distances, zero-damage and fixture-nonmutation assertions; the entity-backed coal collection returns to the GameTest-managed origin, keeps its target 2.5 blocks away so real movement and `playerTouch` remain required, and waits on real server ticks before exact inventory verification because Forge, like Fabric, retires ordinary fixture entities outside the managed structure | successive failure logs prove external `ENVIRONMENT_HAZARD` death in retreat/swim, follow `STUCK` outside the original corridor, transient `inWall` damage from a redundant low inner roof, same-callback `RESOURCE_INSUFFICIENT` before the coal entity reached the live index, and `RESOURCE_DISAPPEARED` when the formerly 4.5-block-away coal survived long enough to initialize but crossed the GameTest cleanup boundary before pickup; the final outer high-clearance arenas, managed moving entity fixture and real-tick terminal poll pass Forge 4/4 twice consecutively with the unchanged 480-tick/three-block/six-block retreat, 220-tick/three-block follow, exact +1 coal delta and zero-health-loss hazard standards | exact-candidate full-chain and remote CI evidence remain in progress |
| Gradle Provider Process Diagnostics | LOCALLY_VERIFIED | the shared Provider-compatible process helper forwards captured stdout and stderr to the Gradle log before retaining nonzero-exit enforcement; release staging separately consumes Provider-owned Git output without duplicating it | the first exact-candidate Runtime/Fabric E2E returned nonzero but the old helper irreversibly suppressed the child error, so its specific external-orchestration cause is honestly `UNDIAGNOSABLE_DUE_SUPPRESSED_CHILD_OUTPUT`; after forwarding was restored, the complete Runtime/Fabric chain passes twice consecutively in approximately 63 and 59 seconds, and any future failure retains its child evidence | remote runner log forwarding remains pending exact-candidate CI |
| GameTest Fixture Ownership Boundary | LOCALLY_VERIFIED | all 30 Fabric mock Owner fixtures now terminate through one cleanup boundary that removes both the Companion and the registered mock ServerPlayer from the shared server; fixture-owned dropped items, projectiles and surviving threats are discarded after their exact assertions; the unknown-Mod menu creates a fresh container per open instead of sharing mutable static inventory; chained Fabric behaviors use non-terminal waits until the final boundary, and the Forge navigation/collection/mining/container chain advances exclusively on real server ticks; mining uses a dedicated forced arena and keeps Owner outside real `ItemEntity` pickup range; registered mock-player UUIDs are immutable; `gameTestFixtureCheck` is part of ordinary `check` and rejects cleanup bypass, mutable static menu containers, missing entity cleanup, premature terminal waits, manual Runtime ticks, post-registration UUID mutation, or loss of the mining pickup boundary | the first systemic run deliberately stopped after exposing the observation fixture's undeclared generated-world sight obstruction (`BLOCK_NOT_VISIBLE`) while the other 30 Fabric cases passed; log reconciliation then proved an apparent 31/31 still created 30 Companions but removed only 29 because the global-navigation callback marked success before its asynchronous chain; a later exact-candidate run stopped at mining when co-located Owner and Companion competed for the two real diamond drops and the behavior correctly waited for the Companion inventory delta; after spatially separating their pickup authority and removing post-registration UUID mutation, the focused real-server suite passes Fabric 31/31 with exactly 30 created/30 removed, no timeout and no mock Owner leak; Forge passes 4/4, dual compilation and the fixture-policy check pass | exact-candidate full-chain and remote CI evidence remain in progress; this is automated GameTest fixture evidence, not Live Brain or human-play evidence |
| Windows Atomic Installer Journal | LOCALLY_VERIFIED | installer journal and manifest JSON are serialized to a same-directory temporary file, explicitly closed and forced to storage before replacement; unsupported atomic moves retain the existing replace fallback, while Windows file-system sharing failures receive one bounded six-attempt primitive-level retry window (20–640 ms) without rerunning an operation or E2E; success and failure paths always remove the temporary file while preserving the primary exception and any cleanup exception | the first standalone Web E2E after the otherwise successful Golden Path failed on a newly written `.mcac-json-*.tmp -> transaction.json` move and left the temporary file; a focused installer test now completes 20 sequential install/update journal replacements and asserts the exact artifact after every revision plus no `transaction.json` or `.mcac-json-*` residue; the corrected complete bilingual real-backend Chromium product flow passes once | exact-candidate full-chain and remote Windows CI remain in progress |
| Postfreeze Final-Gate Root-Cause Closure | LOCALLY_VERIFIED | the Fabric cold-cache boundary uses exact local-coordinate trust plus signed canonical remote artifacts; Forge and NeoForge carry exact stable Gradle module/POM checksums generated by official Gradle verification mode; sensitive-file creation separates new-file ownership establishment from existing-file fail-closed validation, so an elevated Windows default owner cannot block a newly created empty file and cannot authorize takeover of a foreign file | candidate `c0e2fe0cf17465c1070efdc6097554cf21659f5d` passed PR fast and supply-chain/CodeQL but its preserved first Windows and heavy runs exposed the elevated-default-owner and Loader-metadata gaps; Forge Cold A write plus independent Cold B read-only artifact creation pass, NeoForge Cold A write plus independent Cold B read-only artifact creation pass, dependency/secret/documentation gates pass, focused owner/pairing/Web tests pass, and the Windows-equivalent prebuilt real-backend Chromium product path passes once | exact final-candidate PR fast, Windows terminal/package, supply-chain/CodeQL, and Minecraft-heavy runs remain required before merge; Live Hermes and human play remain explicitly pending |
| 0.3.1 Freeze and Release Closure | RELEASED_WITH_EXPLICIT_GATE_OVERRIDE | final repair candidate `5235fbb18f35c5ad7fff8ef207ff02a540588db9` was ordinarily merged through PR #26; annotated tag `mcac-productization-baseline-0.3.1` targets merge SHA `747c7e8046073d9534eae6ae775645341be4cdcd`; the published Manifest names that exact source commit and the Release contains the ZIP, external sidecar, Manifest, SPDX SBOM and SHA list | targeted local documentation and Gradle 9 probe passed; PR Fast `30436629059`, Supply chain/CodeQL `30436629064`, and the earlier Heavy run `30433326289` passed; the user explicitly instructed the closeout not to wait for or rerun project gates, so running Windows workflow `30436629017` is not claimed as successful evidence | `LIVE_BRAIN_EXTERNAL_VERIFICATION_PENDING`; `HUMAN_PLAYTEST_PENDING`; prior Phase-8 “remaining” cells are historical premerge checkpoints superseded only for merge/tag/Release state by this row |

| 2026-08-02 Reliability and Live-Evidence Closure | LOCALLY_AND_LIVE_VERIFIED | Public Task, Task Graph and Skill starts share one durable asynchronous receipt contract; accepted executions are not transport-timeout cancelled and External-Brain tracking is bounded to 64 active handles per Companion without cancelling a still-valid execution solely because it is old. Terminal REST/SSE/errors, Doctor evidence, persisted Smoke results and atomic Support Bundles share server-side privacy handling and bounded mixed-encoding log decoding. Production entry points no longer bind the legacy internal Agent types. The Windows console launcher writes bounded bootstrap diagnostics and no-argument launch opens the control page. Forge uses a Gson-backed bridge JSON layer with no production Jackson references and valid Minecraft 1.20.1 pack metadata. A shared Loader source set packages the body-control arbiter into every Loader artifact and exposes structured control transitions. Product truth records only the exact verified live combination. | Root `check` passes; Web unit/build passes; three Loader production builds pass; final Forge artifact inspection requires the JSON compatibility, body-arbiter classes and `pack.mcmeta` format 15 while rejecting Jackson references; real Fabric 32/32 and Forge 5/5 GameTests exercise owner/runtime/reflex arbitration; authenticated Runtime/Forge E2E passes handshake, FOLLOW, pause/resume/cancel, startup-log Jackson scanning, and rejects ResourcePackInfo/missing-data-pack warnings; the final Windows image test passes no-argument launch from a Unicode/space path, port-conflict bootstrap diagnostics, and pre-JVM missing-DLL visibility. Independent free simulation is `SKIPPED_BY_USER_REQUEST` and is not relabeled as regression or E2E evidence. Historical sanitized 2026-08-01 evidence verifies PCL + Forge 1.20.1 + Runtime + Hermes + official DeepSeek; current 2026-08-02 real PCL evidence is limited to the closeout facts above because official DeepSeek credentials were absent. | `HUMAN_PLAYTEST_PENDING`; official-DeepSeek closeout, Fabric/other-provider live combinations, broad Tool/complex-terrain coverage, common third-party Mod packs, production-duration multi-profile load, and sustained subjective play remain outside this bounded closure. |

| 2026-08-09 Runtime and Terminal Reliability Repair | LOCALLY_VERIFIED | Fabric and Forge fence every asynchronous WebSocket callback by a packaged connection epoch and share the exact nested `START_BEHAVIOR.arguments.parameters` selector; transient player and behavior state is bounded and cleared on disconnect. Runtime Companion rebinding atomically revokes the old Session and command/event/ACK/player/activity ingress uses the authoritative mapping. Operation Plan confirmation is atomically single-consume and retry-recovers one Operation. Compatibility rollback reuses activation environment/dependency/conflict/health validation before switching the signed index. Terminal instance fallback, stale-resource suppression, cancellable bounded Operation polling, malformed-event isolation, finite SSE reconnect, safe external URLs, coordinate/numeric validation and awaited E2E teardown are enforced; root `check` includes Web lint and production build, while Windows Layer 2 watches the release-documentation and final-artifact boundaries. NeoForge remains unchanged as `LOCAL_ONLY`. | Focused core, Runtime, Compatibility and Terminal tests pass, including connection races, nested Runtime parameter selection, two-Session rebinding/late disconnect, incoming sequence semantics, 24-way Plan confirmation and rollback refusal. Root `check` passes with 15 Web test files / 23 tests plus lint, production build, documentation positive/negative, supply-chain, secret and fixture gates. Fabric 1.21.1 and Forge 1.20.1 production builds and their strengthened bridge-class artifact checks pass. The complete bilingual packaged-Terminal real-backend Chromium flow passes 1/1 and both its successful and preserved failure paths exit normally. Authenticated Runtime/Forge E2E passes handshake, FOLLOW, pause/resume/cancel and startup/pack diagnostics after first exposing and then closing the shared-class packaging gap. Runtime/Fabric E2E passes the live Registry/primitive, ASK_USER, pause/restart, abrupt-crash no-replay, inventory/menu and Replay external-Brain chain after its restart assertion was aligned with the durable asynchronous `TASK_GRAPH_ACCEPTED` receipt contract. | This is local automated unit/integration/browser/GameTest evidence, not current Live-provider, free-simulation, long-soak, broad third-party-Mod, complex-terrain, subjective experience or human-play evidence. `LIVE_BRAIN_EXTERNAL_VERIFICATION_PENDING`; `HUMAN_PLAYTEST_PENDING`. |

## Remaining external verification

- `HUMAN_PLAYTEST_PENDING`

The exact live-provider slice recorded above is complete. Broader combinations remain limitations,
not a reason to relabel that verified slice as pending.

## Automated productization freeze checkpoint

Post-merge main `cb5ddfee5773d74cf4fa970eb29221d6d0223e77` rebuilt the Release with an exact
`sourceCommit` match and passed PR/main fast `30290340793`, Windows terminal/package
`30290340461`, Minecraft heavy `30290340900`, and supply-chain/CodeQL Java plus
JavaScript/TypeScript `30290341011`. The Windows chain passed after one audited retry of an earlier
main SHA whose Forge Maven TLS handshake was terminated before product tests. The first
post-merge CodeQL Java run exposed that a restored Gradle cache could result in no observed
compilation; the workflow now forces a no-cache task rerun, and the corrected analysis passed.

The final documentation-only closure commit is the target of annotated tag
`mcac-productization-baseline-0.3.0`. Its exact-SHA runs and final Release/Manifest/SBOM hashes are
recorded in that immutable tag annotation and the final delivery; they cannot be embedded into the
commit before the commit and its runs exist.

## Historical implementation order

This list records the original build sequence only. It is not the current task queue; the active
Fabric-first product-finish contract and release gates determine remaining RC work.

1. Implement durable deterministic Task Graph execution and checkpoint/resume.
2. Complete the primitive Tool families and migrate composite capabilities onto shared executors/skills.
3. Implement the isolated Agent Workspace and generated Skill quarantine/promotion lifecycle.
4. Prove generality through primitive-only representative E2E, fuzz/failure injection and unknown Registry content.
5. Finish UI, MCP identity/reconnect hardening, memory/search contamination protection and current-version release validation.
