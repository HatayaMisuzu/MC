# RC completion matrix

Updated: 2026-07-18
Baseline: `3cc7861`
Overall status: `PARTIAL`

Status values are limited to `NOT_STARTED`, `PARTIAL`, `IMPLEMENTED`, `LOCALLY_VERIFIED`,
`REMOTELY_VERIFIED`, and `BLOCKED_BY_EXCLUDED_LIVE_TEST`. Replay evidence is not Live evidence.

| Module | Status | Implementation evidence | Test evidence | Remaining gap |
|---|---|---|---|---|
| Primitive Tool Coverage | PARTIAL | Existing gateway exposes bounded world observation/scan, movement, composite resource/inventory/item/combat actions and deterministic task controls; read-only primitives add bounded `world.query`, `inventory.inspect`, `safety.inspect`, `task.inspect`, dynamic `capability.list/describe`, capability-gated live `registry.search/describe` plus `recipe.query`, and connected-body `block.inspect`, `item.inspect`, and `entity.inspect`; bounded mutation/body-control entry points add `movement.step/look/stop`, `block.break`, `entity.collect`, and bidirectional `inventory.transfer` without adding scenario Handlers, reusing durable lease/task state and the existing PlayerAction, TRAVEL, mining, pickup and verified-container executors | Runtime primitive tests cover unknown namespaced inventory/container data, observation envelopes, hazard flags, task/capability lifecycle, query capability gating, argument/session binding, cancellation and full MCP-to-authenticated-Mod Registry/Observation result routes; 15/15 real Fabric GameTests cover the shared body executors, live unknown-namespace Registry/recipe queries, visible live block/item/entity inspection, and verified vanilla body rotation | Complete block interact/place, inventory drop, item use, entity interact/attack, menu, retreat and wait/checkpoint primitive families; add cross-loader Registry support, direct per-alias Fabric validation, cancellation/budget breadth and remaining composite equivalence |
| Task Graph Schema | LOCALLY_VERIFIED | `mcac-task-graph/1` JSON/YAML codec; all required declarative node types; stable node IDs; typed inputs; permissions; bounded limits including separate Evidence entry/byte ceilings; JSON object and real `document+format` JSON/YAML Tool ingress | YAML/JSON parsing, Tool-ingress YAML execution, duplicate-key, oversized-document and invalid Evidence-byte-limit tests | Remote verification and schema migration/versioning |
| Task Graph Validator | LOCALLY_VERIFIED | strict unknown-field rejection, safe expression parser without eval, unique IDs, contextual Tool availability, explicit current-Runtime node executability, graph-to-current `ToolDefinition.permission` and `inputSchema` binding, unknown-permission rejection, bounded literal array-reference validation, nested-parallel rejection and hard ceilings | Runtime integration verifies unsupported-node, missing/mismatched/unknown Tool permission, required/type/range/enum/additional-property schema mismatches, safe deferred references, malformed/dynamic/over-limit array reference and nested-parallel rejection plus prior fuzz-shaped boundary cases | expression/schema validator fuzz/property coverage and remote verification |
| Task Graph Runtime | PARTIAL | `task_graph.execute` creates a persistent asynchronous execution; `inspect/pause/resume/cancel` are session-owned controls and external cancel results preserve the request `callId`; timeout requests cancellation of every active child Tool, waits for bounded durable confirmation, and persists unconfirmed cancellation as `RECONCILIATION_REQUIRED`; recovery `resume` rechecks graph hash, persisted state shape, current Tool availability/permission/schema/idempotency and owner binding before leaving reconciliation; deterministic nodes now include `if`, `switch`, `repeat`, `while`, durable `ask_user`, permission-bound `read_memory`, and quarantined `suggest_memory`; safe expressions read inputs, persisted state and prior Tool observations without eval; array `.length` and literal `[0..255]` selection compose observations into later Tool arguments; resolved arguments are revalidated against the current Tool schema immediately before dispatch; all graphs share one Runtime-owned four-worker parallel pool, each graph/node is capped by `maxParallelNodes`/`maxConcurrency`, nested parallel is rejected, and close confirms worker termination; stable scoped Tool IDs, runtime permission recheck, dedupe, serialized concurrent snapshots, unified Evidence entry/byte rotation, durable node outputs and terminal return values are enforced; oversized Evidence entries retain explicit size/hash metadata without raw content; Runtime performs no planning | asynchronous start/progress/inspect, YAML, safe expression, input/output/state and array reference, static and resolved Tool-schema rejection without dispatch, unified progress/Tool/checkpoint Evidence byte+entry budget, unknown-namespace scan-candidate-to-navigation composition, conditional/switch branching, bounded loop exhaustion, two-Graph shared global concurrency cap, true concurrent branch entry, all-active-Tool cancellation, confirmed wait cancellation, unconfirmed Tool cancellation quarantine, external cancel call identity, typed memory filtering/permission denial and suggestion isolation, durable game/HTTP answer routing, terminal-result query, pause/resume, idempotent restart recovery, confirmed-result no-repeat, corrupt-state rejection, duplicate call ID and cross-session isolation tests | scheduler fairness/soak/performance evidence, live Mod result reconciliation and exact scoped retry/loop effect reconciliation |
| Task Checkpoint/Resume | PARTIAL | migrations 11–15 and repository persist graph/input/hash/version, identity, current node, completed scoped nodes, Tool results, variables, all node outputs, loop cursors, checkpoints, bounded evidence, Task Graph-owned waiting questions, terminal result, permissions, limits, provenance and revision; every supported node/Tool/checkpoint/loop/parallel/question boundary writes state; safe pause and durable user answers resume from completed scoped node IDs, loop cursors and immutable Tool results without repeating effects; startup preserves safely `WAITING`/`PAUSED` executions and marks uncertain `READY`/`RUNNING` work `RECONCILIATION_REQUIRED`; explicit recovery resumes an unconfirmed idempotent Tool or reuses a confirmed completed result, while non-idempotent unknown effects remain quarantined | repository optimistic-revision/restart/result tests, injected Tool transport failure, Runtime pause/resume tests at sequence and in-loop Tool boundaries, ASK_USER restart/resume, idempotent restart continuation, confirmed non-idempotent no-repeat, graph-hash/Evidence-shape rejection, duplicate-answer rejection and waiting-question cancellation | live Tool/Mod status reconciliation, exact non-idempotent retry/loop-scope result matching and broader crash fault injection |
| Agent Workspace | PARTIAL | Runtime-owned logical resource store isolates hashed profile/companion scopes, exposes no host path, allows only `.yaml/.yml/.json/.md`, enforces 128-file/2 MiB/64 KiB quotas, rejects traversal/absolute/drive/backslash/ADS/reserved/control/non-NFC paths, verifies every existing path against symlink/reparse/scope escape, atomically replaces content/index, versions changed content, records SHA-256 and keeps Runtime-only previous-version backups | local workspace isolation/version/hash/quota/path attack tests; symlink escape test is present but explicitly skipped on this Windows host because the test process cannot create links | remote link/junction coverage, migration/restore tooling, backup retention policy and terminal Workspace UI |
| Built-in Skills | PARTIAL | read-only classpath catalog exposes six declarative Task Graph compatibility Skills for existing collect, mine, smelt, withdraw, craft and defend composites; IDs cannot be overwritten or promoted, and `skill.execute` runs them through the persistent Task Graph Runtime rather than a new Handler | all resources parse and validate against matching current Tool permissions; fake Tool integration proves one built-in persists and executes through Task Graph with built-in provenance | replace compatibility wrappers with primitive compositions as Primitive coverage lands, add catalog/UI presentation and remote verification |
| Generated Skill Quarantine | PARTIAL | `skill.list/read/save_draft/validate/request_promotion/execute` operate only on logical Skill IDs; save is quarantined, validation uses the real bounded JSON/YAML codec plus current executable node, Tool permission and input-schema contracts, promotion requests persist Brain/controller provenance, permissions, validation evidence and SHA, and only the ACTIVE approved version may execute through Task Graph; generated Skills cannot call `skill.*` recursively | local Skill gateway save/read/list/isolation, valid/invalid graph, pending-review, pre-approval execution rejection, approved asynchronous execution and provenance tests | untrusted-content scanning beyond graph/schema validation, temporary-use policy and terminal quarantine review |
| Skill Promotion | PARTIAL | migration 17 persists numbered review records and document content/hash; the Brain can only request promotion, while authenticated loopback management approves/rejects as `LOCAL_MANAGEMENT_USER`; approval supersedes the prior ACTIVE version, `skill.disable`/`skill.rollback` can only act on previously approved versions within the same profile/companion, and `skill.execute` loads only ACTIVE content with a fresh SHA check | repository approval/rejection boundary, dedupe, two-version supersession, disable, approved rollback and cross-companion/pending rejection tests; Runtime authenticated HTTP approval and approved execution integration | terminal approval/rejection UI, explicit configurable Promotion Policy, cancellation/revocation propagation to an already running graph, signature/administrator policy and remote verification |
| Registry/Mod Compatibility | PARTIAL | Fabric 1.21.1 advertises capability-gated live Registry/recipe/spatial-observation queries; Runtime binds bounded results to query ID, authenticated Runtime session, companion and Brain call; ITEM/BLOCK/ENTITY/MENU and loaded DIMENSION discovery, crafting/smelting recipes, and visible block/item/entity state use the connected server instead of per-Mod adapters | Gateway tests cover capability hiding, schema bounds, cancellation and cross-session rejection; Runtime integration proves MCP dispatch and authenticated WebSocket result routing; isolated Fabric GameTests register an unknown `mcac_registry_fixture` item/block namespace and prove live search/describe, recipe-manager observation, unknown block inspection, connected inventory counts, and visible entity filtering | Add Forge/NeoForge bridge support, tags/tool requirements/components and broader generic interaction/menu E2E |
| Primitive-based Representative E2E | NOT_STARTED | — | — | Five structurally different external-Brain Task Graph E2Es, including unknown namespace content |
| Existing Composite Capability Migration | PARTIAL | Existing composites remain bounded compatibility entries; `movement.step/look/stop`, `block.break`, `entity.collect`, and `inventory.transfer` now route into the same durable command/action executors rather than new Java scenario Handlers, while declarative built-in Skills remain available for larger compatibility flows | Runtime protocol-capture tests prove alias-to-shared-executor mapping with unknown namespaced content; real Fabric GameTests verify shared movement/rotation/mining/pickup/container effects | Migrate remaining composites onto completed primitive compositions or built-in Skills and add direct Runtime/Fabric equivalence E2E |
| External Brain Bridge | REMOTELY_VERIFIED | `runtime/.../brain`, authenticated `/brain`, game/Web ingress | PR fast, Windows, Minecraft heavy green at baseline `86e8262` | Live provider verification excluded |
| Async Tool Loop | LOCALLY_VERIFIED | durable session/call/task/behavior binding; immediate PAUSED/RECONCILIATION mapping; timeout cancellation confirmation; unconfirmed Minecraft Tool cancellation is persisted and returned as `RECONCILIATION_REQUIRED`; audited callId dedupe | PAUSED/reconciliation/confirmed-timeout/unconfirmed-timeout, duplicate-call, concurrent-cancel, companion-isolation and restart tests; prior real Fabric E2E | Remote verification of current SHA |
| Hermes Resume | LOCALLY_VERIFIED | same-session resume, durable question recovery, and once-only undelivered observation delivery | coordinator interruption/resume and audit restart tests | Remote disconnect/backoff E2E and protocol doctor |
| External ASK_USER | LOCALLY_VERIFIED | structured Hermes/OpenAI-compatible question; durable brainSession/task binding; Web/game routing; idempotent answer | adapter/restart/HTTP/game Replay plus real Fabric `6/16 -> ASK -> withdraw 6 -> return -> deliver` E2E | Remote verification of current SHA; Live/Human excluded |
| Search | PARTIAL | bounded query/open/citations/cancel gateway and privacy filtering; five-minute/128-entry in-memory cache is companion+policy scoped, issues independent sessions on hits, clears the relevant entry on cancel, and clears fully on shutdown; Terminal/Web configuration covers explicit enable/disable, HTTPS-or-loopback endpoint, environment-only token, 1..30s timeout, and non-overlapping allow/deny domain policy; pairing writes validated policy into `runtime.yml`; general Doctor checks safe-disabled/configured token-source readiness and the Search page runs an explicit bounded live provider probe without exposing credentials or response bodies | Runtime Replay and cache-isolation/cancel tests; Search configuration/YAML propagation and live-probe contract tests; Search page Vitest; production Web build | source-click UI, persisted/full search-session lifecycle and remote/live provider verification |
| Memory | PARTIAL | four fact types, provenance, user CRUD/export; `memory.search` accepts an optional typed kind filter and Task Graph `read_memory` preserves source/verified metadata; migration 16 adds a separate expiring `memory_suggestion` quarantine, generic `memory.suggest`, a legacy preference wrapper, and Task Graph `suggest_memory` without writing `memory_fact` | repository isolation, typed/sensitive suggestion Tool, Task Graph permission/filter/quarantine and existing Web tests | working-memory cleanup, episodic automation, conflict/capacity policy, user review/promotion/rejection and suggestion UI |
| LocateKnownContainer | LOCALLY_VERIFIED | bounded `world.locate_known_container` returns only body/user-verified World Memory and flags cross-dimension candidates | multi-container, cross-dimension, unverified-filter Runtime tests | remote verification of current SHA |
| Container | LOCALLY_VERIFIED | vanilla withdraw/deposit, verified deltas, and known-container lookup | Fabric GameTests and Runtime/Fabric E2E | multi-container fill, invalidation/restart breadth |
| Craft | LOCALLY_VERIFIED | vanilla 2x2/3x3 recipe/menu execution | Fabric GameTests | broader recipe/restart cases |
| Explore | PARTIAL | `world.scan` dispatches incremental `ExploreArea`: radius 1..16, 256 blocks/tick, loaded chunks only, 64 candidates, distance ranking | Runtime schema test; real Fabric GameTest proves asynchronous scan and ranked ore candidates with a batch-unique fixture | route exploration, world-change/unreachable/restart breadth, Runtime/Fabric Brain E2E |
| Collect | PARTIAL | `resource.collect` uses walking input plus vanilla `ItemEntity.playerTouch`; validates inventory delta, missing/insufficient/full/no-progress states | Runtime schema test; isolated real Fabric GameTest uses a bounded support surface, collects a coal stack after movement, and proves entity removal + inventory delta; full cross-loader GameTest passes | scattered/moving drops, restart/unreachable breadth, wood/iron/diamond cases, Brain E2E |
| Mine | PARTIAL | `resource.mine_vein` dispatches bounded `MineResourceVein`; discovers a connected six-neighbor vein, accumulates vanilla hardness progress, breaks through `ServerPlayerGameMode`, and collects actual drops before accepting verified inventory deltas | Runtime schema test; isolated real Fabric GameTest mines two supported connected diamond ores, verifies both world changes, two vanilla drops, iron-pickaxe durability >=2, and audited action-path evidence; full cross-loader GameTest passes | navigation to distant veins, mixed/common resources, tool break/full inventory/world change/restart breadth, Brain E2E |
| Smelt | PARTIAL | `item.smelt` resolves a vanilla smelting recipe, requires an empty verified furnace in reach, transfers held input/fuel through `FurnaceMenu`, waits for the real cook lifecycle, retrieves the result through the menu, and verifies the output inventory delta | Runtime schema boundary test; real Fabric GameTest smelts two raw iron with coal, verifies input consumption, result pickup, cleared input/result slots, observation and action evidence | fuel-duration preflight, alternate furnace/recipe/fuel breadth, busy/full/fuel-loss GameTests, cancellation/restart and Brain E2E |
| Retreat | PARTIAL | Runtime-independent pre-task reflex detects a live hostile within 3 blocks, interrupts any active navigation/skill through the normal action gateway, walks directly away with player input, requires >=3-block displacement and >=6-block threat clearance, then leaves the interrupted work explicitly PAUSED | isolated real Fabric safety GameTest starts travel toward a no-AI zombie, proves task interruption, increased hostile distance, movement delta, terminal observation and successful action evidence; full 11-test suite passes | terrain-aware escape planning, fire/lava/drowning directional recovery, multiple/ranged threats, restart and Runtime/Fabric Brain E2E |
| Defend | PARTIAL | `combat.defend_owner` is exposed only through the Runtime/Fabric capability intersection; selects one live hostile within 8 blocks of the owner, approaches with player input, honors vanilla attack cooldown, attacks through `ServerPlayer.attack`, and completes only when the threat dies or remains outside the 10-block protection radius | Runtime zero-argument schema test; isolated real Fabric combat GameTest kills a one-health husk, earns vanilla Monster Hunter advancement, consumes sword durability, and verifies terminal observation/evidence; full 12-test suite passes | hostile intent/ownership policy, multiple/ranged threats, armor/weapon strategy, friendly-fire breadth, cancellation/restart and Brain E2E |
| Multi-strategy | PARTIAL | External Brain can chain verified tools | container chain Replay E2E | route/resource/tool/fuel/diamond strategy E2E |
| MCP / Tool Protocol | PARTIAL | authenticated `/mcp` Streamable HTTP JSON-RPC endpoint negotiates MCP `2025-03-26`/`2025-06-18`, enforces the negotiated version, exposes only context-filtered MCAC Tool Gateway definitions, binds calls/cancellation to controller + Brain session + companion, streams token-bound `notifications/progress`, cancels disconnected streams, and returns structured terminal observations; `docs/MCP_PROTOCOL.md` covers lifecycle and Hermes connection contract | Runtime HTTP integration verifies auth, negotiation/version rejection, required binding headers, bounded tool list, rejected missing companion, JSON/SSE terminal responses, stable call identity and cancellation notification; protocol unit test verifies progress envelope/token; Terminal Doctor live-probes initialize + bounded tools/list | SSE event resumability, live concurrent cancellation E2E, and verified Hermes-native configuration |
| Web UI | PARTIAL | chat, Brain audit, memory/conversation views, Provider configuration, and Search/privacy configuration | Vitest, TypeScript production build and existing Playwright flow | Search sources/clicks, tool-progress controls, remaining privacy/permission completeness |
| Game UX | PARTIAL | natural reply outbox, legacy waiting question delivery, and Task Graph-owned ASK_USER answers routed back to the original execution without consuming unrelated chat | Runtime/Fabric conversation evidence plus local WebSocket/HTTP Task Graph answer routing integration | External Brain and Task Graph progress presentation, human playtest and remote verification |
| Reliability | PARTIAL | WAL, leases, restart interruption, dedupe; External Brain cancellation removes session ownership before waking an active Tool so the resumed turn cannot race into a false `WAIT`; Task Graph and Minecraft Tool timeouts require bounded cancellation confirmation and quarantine unconfirmed effects for reconciliation; Runtime/Fabric inspection polling tolerates an isolated transport timeout while retaining bounded 10..20s scenario deadlines and final-error evidence; releasing a lease observed just before renewal removes the equivalent renewed in-process bearer instead of leaking an invalid lease into the next Tool; WebSocket delivery audit tests wait for bounded post-send persistence; isolated loader builds are ordered after root `check` and serialized to prevent Windows shared-JAR deletion races while ordinary module tests remain parallel | restart/persistence/E2E tests; concurrent Brain cancel regression repeated 20/20 locally; focused lease renewal/release race regression; Task Graph and Minecraft Tool confirmed/unconfirmed cancellation tests; async delivery audit regression repeated 3/3 locally; local 14/14 real Fabric GameTests and Runtime/Fabric Hermes chain including final delivery; exact `gradlew check buildPlatforms` previously passes | long-run, reconnect/backoff, leak/performance breadth |
| Installer | PARTIAL | Windows installer components exist | installer unit tests | clean-directory full product smoke |
| Migration | PARTIAL | versioned SQLite migrations through migration 17, including durable Task Graph outputs, exclusive Task Graph waiting-question ownership, separate memory suggestion quarantine, and generated Skill review/version state | migration rollback/idempotency tests plus Runtime database/repository restart coverage | configuration migration, upgrade backup proof and release-to-release install migration smoke |
| Upgrade | PARTIAL | terminal upgrade components exist | limited unit coverage | atomic end-to-end upgrade verification |
| Rollback | PARTIAL | rollback components exist | limited unit coverage | failed-upgrade full rollback smoke |
| Uninstall | PARTIAL | uninstall support exists | limited unit coverage | process/port/data retention clean smoke |
| Doctor | PARTIAL | diagnostics modules and health endpoints; live MCP negotiation/schema/permission probe | MCP Doctor fake-server contract test plus existing unit/CI coverage | Brain and Search protocol doctors |
| Support Bundle | PARTIAL | diagnostics/support infrastructure exists | CI coverage | privacy-redacted failure bundle full verification |
| Release Package | PARTIAL | platform artifacts build | `buildPlatforms` | complete RC package, SBOM, manifest and SHA alignment |
| CI | REMOTELY_VERIFIED | three GitHub Actions workflows | `3cc7861`: PR fast `29494869050`, Windows `29494870535`, and heavy `29494868993` green; a later scheduled heavy rerun `29608119914` exposed the lease renewal/release race now fixed locally | push the current fix and Observation slice, then require all workflows green on the resulting SHA |

## Excluded final verification

- `LIVE_BRAIN_EXTERNAL_VERIFICATION_PENDING`
- `HUMAN_PLAYTEST_PENDING`

## Current execution order

1. Implement durable deterministic Task Graph execution and checkpoint/resume.
2. Complete the primitive Tool families and migrate composite capabilities onto shared executors/skills.
3. Implement the isolated Agent Workspace and generated Skill quarantine/promotion lifecycle.
4. Prove generality through primitive-only representative E2E, fuzz/failure injection and unknown Registry content.
5. Finish UI, MCP identity/reconnect hardening, memory/search contamination protection and current-version release validation.
