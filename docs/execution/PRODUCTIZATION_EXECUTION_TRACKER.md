# Productization execution tracker

Status: Superseded / Historical. This file is not current status. Current truth:
`docs/PRODUCT_STATUS.md` and `docs/RC_COMPLETION_MATRIX.md`.

Updated: 2026-07-28
Branch: `main`
PR: `#3`
Recovery starting head: `ca91fd767c9015f3e3d397a348f35ebcbd389be3`

This is an execution tracker, not a second completion matrix.
[`docs/RC_COMPLETION_MATRIX.md`](../RC_COMPLETION_MATRIX.md) remains the only authoritative
completion matrix. Status values here are `COMPLETED`, `IN_PROGRESS`, `INTERRUPTED`,
`NEEDS_REVERIFICATION`, and `NOT_STARTED`.

## Reconstructed task list

| Order | Work item | Status | Exact evidence or next acceptance condition |
|---:|---|---|---|
| 1 | Recover the existing PR/branch without discarding prior work | `COMPLETED` | Branch and PR #3 were retained; no reset/rebase/restart from old `main`; committed head is `ca91fd7`. |
| 2 | Productization safety/reliability rescan | `COMPLETED` | `16fd1c8`: bounded planning queues, lock stripes, HTML-window limits and corrected Fabric drop fixture; pushed and remotely checked. |
| 3 | Compatibility Host foundation | `COMPLETED` | `dc4d530`: bounded declaration-only loader, immutable Store, signed index, journal recovery, fingerprint, resolver, scoped grants, lifecycle API, built-in Loader descriptors and Terminal page; focused local tests passed and commit was pushed. |
| 4 | Full `zh-CN` / `en-US` Terminal localization and terminology | `COMPLETED` | `ca91fd7`: full resource catalogs, persistent no-refresh switch, missing-key tests, localized API errors, terminology document and capability-source UI; 10 Web test files / 15 tests and production build passed before push. |
| 5 | Repair old Windows browser selector failure | `COMPLETED` | Root cause was CI system locale after i18n, not a backend outage. The browser flow now selects locale explicitly and no longer relies on host language. Current uncommitted test must still pass as part of item 7. |
| 6 | Repair real compatibility API serialization | `COMPLETED` | Real browser exposed HTTP 400 because `Instant` lacked Java Time support. Terminal now registers `JavaTimeModule`, emits ISO-8601 text and has `WebTerminalJsonTest`. The new unit test still needs execution in item 7. |
| 7 | Historical packaged-backend compatibility button lifecycle | `COMPLETED` | The frozen 0.3.1 bilingual Chromium path exercised the former install/evidence/index/activate/update/rollback/quarantine/disable/remove controls without a `fetch` mock. The 2026-07-29 corrective audit supersedes that unsafe evidence control; current browser coverage proves install/update remain honestly `STAGING` and permits quarantine/removal only, while trusted lifecycle evidence remains Host automation. |
| 8 | UI interaction matrix and bilingual screenshots | `COMPLETED` | Markdown/JSON matrices contain 33 rows and the JSON parses. Fresh `zh-CN` and `en-US` screenshots were generated by the passing real-backend flow and visually checked for layout overflow, stale Loader metadata and leaked `undefined` values. |
| 9 | RC matrix and resumable checkpoint alignment | `COMPLETED` | The sole RC matrix and compatibility/UI checkpoint now record the exact bilingual real-backend evidence and retain Live Hermes/human play as explicit post-freeze follow-ups. |
| 10 | Documentation single source of truth and historical-document governance | `COMPLETED` | Added current documentation navigation, product status, bilingual user guides, developer entry and archive index; corrected Fabric/Forge and Compatibility Host claims; historical logs remain available but cannot override the sole RC matrix. `documentationCheck` passes. |
| 11 | Repository productization/security/dependency/UI/release cleanup audit | `COMPLETED` | `docs/product/REPOSITORY_PRODUCTIZATION_AUDIT.md` records concrete documentation, UI/API, CI portability, generated-state, skip/debug, legal, dependency and secret findings. Reachable defects were fixed; secret/dependency/docs/npm audit and tracked-state scans pass. |
| 12 | Full local candidate gates | `COMPLETED` | The final working tree passed `clean check buildPlatforms`; all Loader launch/GameTests; Fabric/Forge Runtime E2E; both restart gates; multi-Profile and Runtime-disabled launch; reconnect and unknown-Mod E2E; 105/200-turn reliability gates; npm audit/lint/test/build; package verification, arbitrary-directory HTML first start and the clean-extraction bilingual Golden Path. A timed-out shell left one duplicate test tree; it was identified by exact repository command line, removed, and the affected launch/GameTest gate then passed cleanly. |
| 13 | Exact candidate Release/Manifest/SBOM/SHA validation | `COMPLETED` | A clean candidate rebuild passed package verification and Golden Path. Independent validation matched all 410 Manifest payload entries by path/size/SHA, all 412 `SHA256SUMS` entries, the external ZIP sidecar, source-bound SPDX 2.3 metadata for 32 versioned/licensed/purl components, required bilingual/compatibility/legal documents, safe ZIP paths, one SLF4J provider and no development/user-state/local-path/secret-shaped residue. The final evidence-only commit is rebuilt once more before push so `sourceCommit` remains exact. |
| 14 | Commit, push and exact-SHA remote CI | `COMPLETED` | Candidate `48f799e8e880858c81a8516c6d92f128bda24b60`: PR fast `30287614857`, Windows terminal/package `30287614849`, Minecraft heavy `30287667164`, and dependency review plus CodeQL Java/JS `30287614859` all passed. |
| 15 | PR #3 review readiness and merge to `main` | `COMPLETED` | PR title/body and evidence were aligned, no review threads existed, all required candidate gates were green, PR was marked Ready and merged normally (not squash/rebase) as `20b3b2eaa87756884e3d96e67737c99554d5b67e`. |
| 16 | Post-merge exact-main rebuild, CI and frozen baseline/tag | `COMPLETED` | Post-merge main `cb5ddfee5773d74cf4fa970eb29221d6d0223e77` rebuilt the source-bound Release and passed fast `30290340793`, Windows `30290340461`, Minecraft heavy `30290340900`, and supply-chain/CodeQL `30290341011`. Baseline and closeout reports are committed; the final documentation-only closure commit is rechecked at its exact SHA and receives the immutable annotated tag. |

## Explicit deferrals

| Item | Status | Boundary |
|---|---|---|
| Dual-loader Live Hermes verification | `NOT_STARTED` by explicit user deferral | `LIVE_BRAIN_EXTERNAL_VERIFICATION_PENDING` / post-freeze only; Replay/Fake/GameTest is never relabeled Live. |
| Human playtest | `NOT_STARTED` by explicit user deferral | `HUMAN_PLAYTEST_PENDING` / post-freeze only; automated Minecraft is not relabeled human play. |

## Recovery decision

Items 1 through 16 are complete. The final documentation-only closure commit is accepted only
after its own source-bound Release and exact-SHA remote checks are green. No current local browser
or Terminal process owns port `32145`.
